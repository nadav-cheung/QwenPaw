# 第 2.2 章 Runner 与请求处理

## 本章导览

| 项目 | 内容 |
|------|------|
| **学习目标** | 1) 描述 AgentRunner 处理请求的完整生命周期 2) 解释三层命令路由的设计原理 3) 分析流水线模式在请求处理中的优势 |
| **前置知识** | 第 1.4 章 |
| **预计时长** | 50 分钟 |
| **难度等级** | ⭐⭐⭐ |
| **核心关键词** | `AgentRunner` `CommandRouter` `请求流水线` |

> **本章概述**：深入分析 Runner 如何处理请求的完整生命周期，包括命令路由、Agent 实例管理、会话持久化和任务追踪的核心设计。

---

## 1. Runner 概述

### 1.1 什么是 Runner

Runner（执行器）负责处理用户请求的整个生命周期，从接收请求到返回响应，是 QwenPaw 与外部系统交互的桥梁。

**核心职责**:
- 请求路由：判断请求类型（命令 vs 普通查询）
- Agent 管理：创建和管理 Agent 实例
- 会话管理：维护对话状态的持久化
- 任务追踪：管理后台任务的执行和状态

**源码路径**: `src/qwenpaw/app/runner/`

### 1.2 Runner vs Agent

| 对比项 | Agent | Runner |
|--------|-------|--------|
| **职责** | 核心智能体逻辑（推理、工具调用） | 请求路由和调度 |
| **位置** | 业务层 | 框架层 |
| **生命周期** | 每次请求新建 | 长驻运行 |
| **核心方法** | `reply()`（ReAct 循环） | `query_handler()`（请求入口） |

**协作关系**:
```
Request → Runner.query_handler() → Agent.reply() → Response
              ↓
        命令路由 / 会话管理 / 任务追踪
```

### 1.3 核心文件架构

| 文件 | 职责 |
|------|------|
| `runner.py` | AgentRunner 核心类，请求处理引擎 |
| `models.py` | Pydantic 数据模型：ChatSpec, ChatUpdate, ChatHistory |
| `session.py` | SafeJSONSession — 异步 JSON 文件会话状态持久化 |
| `command_dispatch.py` | 命令路由：daemon > control > conversation |
| `task_tracker.py` | SSE 流式输出、后台任务追踪、断线重连缓冲 |

---

## 2. AgentRunner 架构

### 2.1 核心类

**源码路径**: `src/qwenpaw/app/runner/runner.py`

```python
class AgentRunner(Runner):
    """请求处理器核心类，继承自 agentscope_runtime.engine.runner.Runner"""

    def __init__(
        self,
        agent_id: str = "default",
        workspace_dir: Path | None = None,
        task_tracker: Any | None = None,
    ) -> None:
        super().__init__()
        self.agent_id = agent_id
        self.workspace_dir = workspace_dir
        self._task_tracker = task_tracker
```

### 2.2 关键设计：每次请求新建 Agent 实例

**核心设计**: 每次请求都创建新的 QwenPawAgent 实例，而非复用。

**优势**:
- 配置变更即时生效（AgentConfig、MCPClients、Skills 都重新加载）
- 无需显式重建 Agent 或重启进程
- 避免状态残留

**开销考量**:
- 大部分时间花在等待 LLM 响应上
- Agent 初始化相对轻量
- 会话状态通过 SafeJSONSession 持久化

### 2.3 依赖注入

AgentRunner 通过 setter 方法注入依赖，实现松耦合：

```python
def set_chat_manager(self, chat_manager):
    """Set chat manager for auto-registration."""
    self._chat_manager = chat_manager

def set_mcp_manager(self, mcp_manager):
    """Set MCP client manager for hot-reload support."""
    self._mcp_manager = mcp_manager

def set_workspace(self, workspace):
    """Set workspace for control command handlers."""
    self._workspace = workspace
```

**设计优势**:
- 依赖倒置：Runner 不直接创建依赖，由外部注入
- 可测试性：便于注入 mock 组件
- 生命周期管理：各组件由外部统一管理

---

## 3. 命令路由

### 3.1 CommandRouter

**源码路径**: `src/qwenpaw/app/runner/command_dispatch.py`

```python
def _is_command(query: str | None) -> bool:
    """True if query is any known command.

    Priority order: daemon > control > conversation
    """
    if not query or not query.startswith("/"):
        return False
    if parse_daemon_query(query) is not None:
        return True
    if _is_control_command(query):
        return True
    return _is_conversation_command(query)
```

### 3.2 三层命令优先级

```
daemon > control > conversation
```

| 层级 | 命令示例 | 处理器 | 用途 |
|------|----------|--------|------|
| **Daemon** | `/daemon status`, `/daemon restart` | DaemonCommandHandler | 系统管理 |
| **Control** | `/stop`, `/model`, `/skills` | StopCommandHandler 等 | 影响当前任务 |
| **Conversation** | `/compact`, `/new` | CommandHandler | 对话管理 |

### 3.3 内置命令一览

| 命令 | 处理器 | 功能 |
|------|--------|------|
| `/daemon status` | DaemonCommandHandler | 查看系统状态 |
| `/daemon restart` | DaemonCommandHandler | 重启服务 |
| `/stop` | StopCommandHandler | 停止当前任务 |
| `/model <name>` | ModelCommandHandler | 切换模型 |
| `/skills` | SkillsCommandHandler | 查看可用技能 |
| `/compact` | CommandHandler | 压缩上下文 |

### 3.4 Control 命令处理

**源码路径**: `src/qwenpaw/app/runner/control_commands/`

```python
class BaseControlCommandHandler(ABC):
    """Control 命令处理器基类"""

    command_name: str = ""

    @abstractmethod
    async def handle(self, context: ControlContext) -> str:
        raise NotImplementedError
```

**Context 传递**:
```python
@dataclass
class ControlContext:
    workspace: "Workspace"
    payload: Any
    channel: "BaseChannel"
    session_id: str
    user_id: str
    args: Dict[str, Any]
```

---

## 4. 请求流水线

### 4.1 流水线 stages

```
1. Parse → 2. Validate → 3. PreProcess → 4. Agent.Run → 5. PostProcess → 6. Response
```

### 4.2 源码实现

**源码路径**: `src/qwenpaw/app/runner/pipeline.py`

```python
class Pipeline:
    """请求处理流水线"""

    async def execute(self, request: Request) -> Response:
        for stage in self.stages:
            request = await stage.process(request)
        return request.response
```

### 4.3 核心处理流程

**query_handler 方法**（12 阶段处理）:

```
Stage 1:  审批检查 → 检查待审批的工具调用
Stage 2:  命令路由 → 判断命令类型，进入对应处理路径
Stage 3:  上下文设置 → 设置 contextvars
Stage 4:  Agent 构建准备 → 收集配置和上下文
Stage 5:  Mission 检测 → 检测 /mission 命令
Stage 6:  Agent 实例化 → 创建 QwenPawAgent
Stage 7:  聊天注册 → 自动注册会话
Stage 8:  技能注入 → 处理 /skillname 命令
Stage 9:  会话加载 → 从 JSON 加载历史状态
Stage 10: 执行 → 流式输出或 Mission 执行
Stage 11: 错误处理 → 异常转换和调试信息
Stage 12: 清理 → 保存状态，更新会话
```

### 4.4 流式输出机制

**可中断的流式输出**:

```python
async def _stream_printing_messages_interruptible(
    agents: list[Any],
    coroutine_task: Coroutine,
) -> AsyncGenerator[tuple[Msg, bool], None]:
    """支持外部取消的流式输出"""
    queue: asyncio.Queue = asyncio.Queue()
    for agent in agents:
        agent.set_msg_queue_enabled(True, queue)

    task = asyncio.create_task(coroutine_task)
    try:
        while True:
            printing_msg = await queue.get()
            if isinstance(printing_msg, str) and printing_msg == _PRINT_END_SIGNAL:
                break
            msg, last, _ = printing_msg
            yield msg, last
    except asyncio.CancelledError:
        await _cancel_streaming_agent_task(task)
        raise
```

**特点**:
- 使用 `asyncio.Queue` 传递消息
- 支持外部取消时立即取消 Agent 任务
- 通过 `task.add_done_callback()` 通知队列结束

---

## 5. 生命周期管理

### 5.1 Runner 生命周期

```
创建 → 启动 → 处理请求 → 停止 → 清理
```

### 5.2 状态机

**源码路径**: `src/qwenpaw/app/runner/state.py`

```python
class RunnerState(Enum):
    CREATED = "created"
    STARTED = "started"
    RUNNING = "running"
    STOPPING = "stopping"
    STOPPED = "stopped"
```

### 5.3 init_handler 与 shutdown_handler

```python
async def init_handler(self, *args, **kwargs):
    """初始化 Runner"""
    # 加载 .env 文件
    # 初始化 SafeJSONSession
    session_dir = str((self.workspace_dir or WORKING_DIR) / "sessions")
    self.session = SafeJSONSession(save_dir=session_dir)

async def shutdown_handler(self, *args, **kwargs):
    """关闭 Runner"""
    # 清理逻辑在各组件的生命周期中处理
```

### 5.4 Session 持久化

**SafeJSONSession** 确保会话状态的可靠持久化：

```python
# 关键特性
- 异步 I/O 不阻塞事件循环
- 原子写入防止数据损坏
- raw_decode 回退处理 JSON 损坏
```

---

## 6. 组件交互关系

```
                    ┌─────────────────────────────────────┐
                    │           AgentRunner               │
                    │         query_handler()             │
                    └───────────────┬─────────────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        ▼                           ▼                           ▼
┌───────────────┐       ┌───────────────────────┐   ┌─────────────────┐
│  ToolGuard    │       │    Command Dispatch    │   │   ChatManager   │
│  审批检查     │       │  daemon>control>conv   │   │  会话自动注册   │
└───────────────┘       └───────────────────────┘   └─────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    ▼               ▼               ▼
            ┌───────────┐   ┌───────────┐   ┌───────────────┐
            │ /daemon   │   │ /stop     │   │ /model,skills │
            │ 命令      │   │ 命令      │   │ 命令          │
            └───────────┘   └───────────┘   └───────────────┘
```

---

## 7. 设计意图

### 7.1 流水线模式优势

1. **清晰分离**: 每个阶段职责明确，易于理解和维护
2. **可扩展性**: 新增阶段只需实现 `process()` 方法
3. **可测试性**: 各阶段可独立单元测试
4. **错误隔离**: 单阶段失败不影响其他阶段

### 7.2 三层命令路由设计

**优先级依据**:
- **daemon**: 系统级操作，需要最快响应
- **control**: 影响当前任务的操作
- **conversation**: 对话管理，可以稍后处理

### 7.3 权衡取舍

| 设计选择 | 优势 | 代价 |
|----------|------|------|
| 每次请求新建 Agent | 热重载即时生效 | 初始化开销 |
| 文件存储 Session | 跨进程可访问 | 性能低于内存存储 |
| 三层命令路由 | 职责清晰 | 增加复杂度 |

---

## 8. 知识检查

1. **分析题**：描述 AgentRunner 处理请求的完整流程，包括 12 个主要阶段。

2. **设计题**：为什么要用三层命令路由（daemon > control > conversation）而不是直接调用？这种设计如何保证系统响应性？

3. **源码题**：分析 `_stream_printing_messages_interruptible` 如何实现可中断的流式输出。

4. **概念题**：解释"每次请求新建 Agent 实例"这一设计选择如何实现配置热重载。

---

## 本章小结

| 要点 | 内容 |
|------|------|
| **Runner 职责** | 请求路由、Agent 管理、会话持久化、任务追踪 |
| **命令路由** | CommandRouter，三层优先级：daemon > control > conversation |
| **请求流水线** | 12 阶段处理：审批 → 命令 → Agent → 执行 → 清理 |
| **核心设计** | 每次请求新建 Agent 实例实现热重载 |
| **状态管理** | SafeJSONSession 支持原子写入和损坏恢复 |
| **生命周期** | CREATED → STARTED → RUNNING → STOPPING → STOPPED |

---

## 延伸阅读

- 进阶：第 2.3 章《消息与会话管理》
- 源码：`src/qwenpaw/app/runner/`
- 深入：第 2.13 章《生命周期管理》
