# 第 2.8 章 MCP 扩展机制

## 本章导览

| 项目 | 内容 |
|------|------|
| **学习目标** | 1) 解释 MCP 协议的通信模型 2) 配置和管理 MCP 客户端 3) 理解热重载和生命周期管理机制 4) 分析 StdIO 客户端的跨任务生命周期解决方案 |
| **前置知识** | 第 1.4 章 |
| **预计时长** | 50 分钟 |
| **难度等级** | ⭐⭐⭐ |
| **核心关键词** | `MCP` `MCPClient` `MCPClientManager` `Model Context Protocol` |

> **本章概述**：深入分析 MCP 扩展机制，讲解如何通过 MCP 协议让 AI 模型与外部工具和数据源交互，包括客户端管理、热重载和跨任务生命周期等核心机制。

---

## 1. MCP 概述

### 1.1 什么是 MCP

MCP (Model Context Protocol) 是一种开放协议，允许 AI 模型与外部服务交互。通过 MCP，LLM 可以调用文件系统、数据库、API 等外部工具扩展自身能力。

```
┌─────────────┐         MCP          ┌─────────────┐
│   LLM Model │ ◄───────────────► │ MCP Server  │
│  (QwenPaw)  │                    │ (Tool Host) │
└─────────────┘                    └─────────────┘
                                         │
                    ┌────────────────────┼────────────────────┐
                    ▼                    ▼                    ▼
              ┌──────────┐        ┌──────────┐        ┌──────────┐
              │ Database │        │  Files   │        │   APIs  │
              └──────────┘        └──────────┘        └──────────┘
```

### 1.2 MCP vs Skill

| 对比项 | Skill | MCP |
|--------|-------|-----|
| 协议 | QwenPaw 私有 | 开放标准 |
| 复杂度 | 简单 | 复杂 |
| 适用场景 | 内置功能 | 外部工具 |
| 生态 | 有限 | npm/PyPI 数千服务器 |
| 热更新 | 不支持 | 支持配置变更自动重连 |

---

## 2. MCP 架构

### 2.1 核心组件

```
MCPClientManager → MCPClient → MCPServer
                                   ↓
                            工具/资源/提示
```

**模块结构**:

```
app/mcp/
├── __init__.py
├── manager.py           # MCPClientManager - 生命周期管理
├── stateful_client.py   # HttpStatefulClient, StdIOStatefulClient
└── watcher.py           # MCPConfigWatcher - 配置变更监听
```

### 2.2 类图

**源码路径**: `src/qwenpaw/app/mcp/`

```
BaseMCPClient (ABC)
├── OfficialMCPClient
└── CustomMCPClient
```

QwenPaw 实现了两种传输模式的客户端：

| 客户端类型 | 传输模式 | 适用场景 |
|-----------|---------|---------|
| `StdIOStatefulClient` | 标准输入输出 | 本地 MCP 进程 |
| `HttpStatefulClient` | HTTP/SSE | 远程 MCP 服务器 |

---

## 3. MCPClient

### 3.1 核心类

**源码路径**: `src/qwenpaw/app/mcp/stateful_client.py`

```python
class BaseMCPClient(ABC):
    """MCP 客户端抽象基类"""

    @abstractmethod
    async def connect(self, server_config: dict) -> None:
        """连接到 MCP 服务器"""

    @abstractmethod
    async def call_tool(self, tool: str, args: dict) -> Any:
        """调用工具"""

    @abstractmethod
    async def list_resources(self) -> list[Resource]:
        """列出可用资源"""

    @abstractmethod
    async def list_tools(self) -> list[Tool]:
        """列出可用工具"""

    @abstractmethod
    async def close(self) -> None:
        """关闭客户端"""
```

### 3.2 StdIOStatefulClient

标准输入输出模式的 MCP 客户端，用于本地进程通信：

```python
class StdIOStatefulClient(StatefulClientBase):
    """stdio 传输的 MCP 客户端"""

    async def connect(self) -> None:
        """启动本地进程并建立连接"""

        # 1. 启动子进程
        self.process = await asyncio.create_subprocess_exec(
            self.command,
            *self.args,
            env={**os.environ, **self.env},
            cwd=self.cwd,
            stdin=asyncio.subprocess.PIPE,
            stdout=asyncio.subprocess.PIPE,
            stderr=asyncio.subprocess.PIPE,
        )

        # 2. 创建 stdio 传输
        context = await stack.enter_async_context(
            stdio_client(stdin=self.process.stdin, stdout=self.process.stdout),
        )

        # 3. 创建 session
        self.session = await stack.enter_async_context(
            ClientSession(read_stream, write_stream)
        )
        await self.session.initialize()
        self.is_connected = True
```

### 3.3 HttpStatefulClient

HTTP 轮询模式的 MCP 客户端，适合远程服务器：

```python
class HttpStatefulClient(StatefulClientBase):
    """HTTP/SSE MCP client with proper cross-task lifecycle management."""

    async def _run_lifecycle(self) -> None:
        while not self._stop_event.is_set():
            async with AsyncExitStack() as stack:
                if self.transport == "streamable_http":
                    # 配置 httpx 客户端
                    http_client = httpx.AsyncClient(
                        headers=self.headers or {},
                        timeout=httpx.Timeout(...),
                    )
                    await stack.enter_async_context(http_client)

                    context = await stack.enter_async_context(
                        streamable_http_client(url=self.url, http_client=http_client),
                    )
                else:  # SSE
                    context = await stack.enter_async_context(
                        sse_client(url=self.url, headers=self.headers, ...),
                    )
```

### 3.4 生命周期

```
创建 → 连接 → 使用 → 断开 → 清理
```

**跨任务生命周期管理** (`_run_lifecycle` 方法，第112-175行):

解决 FastAPI/uvicorn 环境中的 CancelScope 问题——原始实现中 `connect()` 在任务 A 进入 AsyncExitStack，`close()` 在任务 B 退出，导致 anyio.CancelScope 错误。

```python
async def _run_lifecycle(self) -> None:
    """在专用后台任务中运行 MCP 客户端生命周期"""
    while not self._stop_event.is_set():
        try:
            async with AsyncExitStack() as stack:
                # 在同一任务中进入上下文管理器
                context = await stack.enter_async_context(
                    stdio_client(self.server_params),
                )
                self.session = ClientSession(read_stream, write_stream)
                await stack.enter_async_context(self.session)
                await self.session.initialize()

                self.is_connected = True
                self._ready_event.set()

                # 等待 reload 或 stop 信号
                while not self._reload_event.is_set() and not self._stop_event.is_set():
                    await asyncio.sleep(0.1)

        except Exception as e:
            logger.error(f"Error in MCP client lifecycle: {e}")
            self.is_connected = False
            await asyncio.sleep(1)
```

**关键设计**:
- **单一后台任务**: 整个生命周期在一个 `asyncio.Task` 中运行
- **AsyncExitStack**: 确保 `connect()` 和 `close()` 在同一任务中
- **_stop_event**: 优雅停止信号
- **_reload_event**: 配置重载信号

---

## 4. MCPClientManager

### 4.1 管理器

**源码路径**: `src/qwenpaw/app/mcp/manager.py`

```python
class MCPClientManager:
    """MCP 客户端生命周期管理器"""

    def __init__(self):
        self._clients: Dict[str, Any] = {}
        self._lock = asyncio.Lock()

    async def init_from_config(self, config: "MCPConfig") -> None:
        """从配置初始化所有客户端"""
        for key, client_config in config.clients.items():
            if not client_config.enabled:
                continue
            await self._add_client(key, client_config)

    async def get_client(self, name: str) -> BaseMCPClient:
        """获取客户端"""
        async with self._lock:
            return self._clients.get(name)

    async def get_clients(self) -> List[Any]:
        """获取所有活跃客户端"""
        async with self._lock:
            return [c for c in self._clients.values() if c is not None]

    async def replace_client(
        self,
        key: str,
        client_config: "MCPClientConfig",
        timeout: float = 60.0,
    ) -> None:
        """热重载：替换客户端"""
        # 1. 连接新客户端（锁外，可能慢）
        new_client = self._build_client(client_config)
        await asyncio.wait_for(new_client.connect(), timeout=timeout)

        # 2. 锁内替换
        async with self._lock:
            old_client = self._clients.get(key)
            self._clients[key] = new_client

        # 3. 关闭旧客户端
        if old_client:
            await old_client.close()

    async def close_all(self) -> None:
        """关闭所有客户端"""
        async with self._lock:
            clients = list(self._clients.values())
            self._clients.clear()

        for client in clients:
            if client:
                await client.close()
```

### 4.2 热重载机制

MCP 支持运行时更新配置而不中断服务，采用"锁外连接，锁内替换"策略：

```
配置文件变更 (config.json)
         │
         ▼
MCPConfigWatcher 检测到变更
         │
         ▼
调用 replace_client(key, new_config)
         │
         ├── 锁外: 创建新客户端
         │       └── new_client = _build_client(config)
         │       └── await new_client.connect()
         │
         └── 锁内: 原子替换
                 ├── old = _clients[key]
                 ├── _clients[key] = new_client
                 └── unlock
                         │
                         ▼
                 锁外: 关闭旧客户端
                         └── await old_client.close()
```

**热重载策略对比**:

| 维度 | MCP 热重载 | Channel 热重载 |
|------|-----------|---------------|
| 锁机制 | `_lock` 内部锁 | `_restart_locks[channel_name]` per-channel 锁 |
| 替换策略 | 锁外连接，锁内替换 | 锁外启动，锁内交换 |
| 资源清理 | `await old_client.close()` | `await old_channel.stop()` |

### 4.3 构建客户端

**源码路径**: `src/qwenpaw/app/mcp/manager.py:230-266`

```python
@staticmethod
def _build_client(client_config: "MCPClientConfig") -> Any:
    """根据传输类型构建 MCP 客户端实例"""
    rebuild_info = {
        "name": client_config.name,
        "transport": client_config.transport,
        "url": client_config.url,
        "headers": client_config.headers or None,
        "command": client_config.command,
        "args": list(client_config.args),
        "env": dict(client_config.env),
        "cwd": client_config.cwd or None,
    }

    if client_config.transport == "stdio":
        client = StdIOStatefulClient(
            name=client_config.name,
            command=client_config.command,
            args=client_config.args,
            env=client_config.env,
            cwd=client_config.cwd or None,
        )
        setattr(client, "_qwenpaw_rebuild_info", rebuild_info)
        return client

    # HTTP 客户端：展开环境变量
    headers = client_config.headers
    if headers:
        headers = {k: os.path.expandvars(v) for k, v in headers.items()}

    client = HttpStatefulClient(
        name=client_config.name,
        transport=client_config.transport,
        url=client_config.url,
        headers=headers or None,
    )
    setattr(client, "_qwenpaw_rebuild_info", rebuild_info)
    return client
```

---

## 5. MCP 配置

### 5.1 配置模型

**源码路径**: `src/qwenpaw/config/config.py:1012-1106`

```python
class MCPClientConfig(BaseModel):
    """单个 MCP 客户端配置"""
    name: str
    description: str = ""
    enabled: bool = True
    transport: Literal["stdio", "streamable_http", "sse"] = "stdio"
    url: str = ""
    headers: Dict[str, str] = Field(default_factory=dict)
    command: str = ""
    args: List[str] = Field(default_factory=list)
    env: Dict[str, str] = Field(default_factory=dict)
    cwd: str = ""

class MCPConfig(BaseModel):
    """MCP 全局配置"""
    clients: Dict[str, MCPClientConfig] = {}
```

### 5.2 服务配置

`~/.qwenpaw/config.json` 中的 MCP 配置：

```yaml
mcp:
  clients:
    filesystem:
      name: filesystem
      enabled: true
      transport: stdio
      command: npx
      args:
        - -y
        - @modelcontextprotocol/server-filesystem
        - /workspace
      cwd: /tmp
    github:
      name: github
      enabled: true
      transport: streamable_http
      url: http://localhost:3000
      headers:
        Authorization: Bearer ${GITHUB_TOKEN}
```

### 5.3 环境变量注入

配置中的 `${VAR}` 会被自动替换为环境变量：

```python
headers = client_config.headers
if headers:
    headers = {k: os.path.expandvars(v) for k, v in headers.items()}
```

这使得配置可以安全引用环境变量而不暴露实际值：

```json
{
  "headers": {
    "Authorization": "Bearer ${GITHUB_TOKEN}"
  }
}
```

### 5.4 传输方式对比

| 类型 | 适用场景 | 优点 | 缺点 |
|------|---------|------|------|
| stdio | 本地 MCP 进程 | 低延迟，无需网络 | 进程管理复杂 |
| streamable_http | 远程 MCP 服务器 | 支持会话恢复，统一端点 | 需要 HTTP 服务器 |
| sse | 远程 MCP 服务器（旧） | 简单 | 无 session 机制 |

---

## 6. MCPConfigWatcher

### 6.1 配置监听

**源码路径**: `src/qwenpaw/app/mcp/watcher.py`

`MCPConfigWatcher` 使用轮询方式检测配置文件变化，实现客户端热重载：

```python
class MCPConfigWatcher:
    """Watch MCP configuration and hot-reload clients on changes."""

    def __init__(
        self,
        mcp_manager: MCPClientManager,
        config_loader: Callable,
        poll_interval: float = 2.0,
        config_path: Optional[Path] = None,
    ):
        self._mcp_manager = mcp_manager
        self._config_loader = config_loader
        self._poll_interval = poll_interval
        self._config_path = config_path

        # 快照：用于快速比较
        self._last_mcp: Optional["MCPConfig"] = None
        self._last_mcp_hash: Optional[int] = None
        self._last_mtime: float = 0.0

        # 重试跟踪：防止无限重试
        self._client_failures: Dict[str, tuple[int, int]] = {}
        self._max_retries: int = 3
```

### 6.2 轮询检查机制

```python
async def _poll_loop(self) -> None:
    """主轮询循环"""
    while True:
        try:
            await asyncio.sleep(self._poll_interval)
            await self._check()
        except Exception:
            logger.exception("MCPConfigWatcher: poll iteration failed")

async def _check(self) -> None:
    """检查配置变化"""
    # 1. 检查 mtime 快速跳过
    if self._config_path:
        mtime = self._config_path.stat().st_mtime
        if mtime == self._last_mtime:
            return
        self._last_mtime = mtime

    # 2. 加载新配置并快速比较 hash
    new_mcp = self._load_mcp_config()
    new_hash = self._mcp_hash(new_mcp)
    if new_hash == self._last_mcp_hash:
        return  # 无变化

    # 3. 检查是否已有进行中的重载
    if self._reload_task and not self._reload_task.done():
        return

    # 4. 在后台任务中触发重载
    self._reload_task = asyncio.create_task(
        self._reload_changed_clients_wrapper(new_mcp),
    )
```

### 6.3 优化手段

| 优化手段 | 说明 |
|---------|------|
| mtime 快速跳过 | 文件修改时间未变则直接跳过 |
| hash 快速比较 | 内容未变则跳过解析 |
| 重载去重 | 已有重载进行中则跳过 |
| 失败重试限制 | 连续失败 3 次后不再重试 |

---

## 7. 实战：配置 MCP 服务器

### 7.1 文件系统服务器

**安装**:
```bash
npx -y @modelcontextprotocol/server-filesystem /workspace
```

**配置**:
```yaml
mcp:
  clients:
    filesystem:
      name: filesystem
      enabled: true
      transport: stdio
      command: npx
      args:
        - -y
        - @modelcontextprotocol/server-filesystem
        - /workspace
```

### 7.2 GitHub 服务器

**配置**:
```yaml
mcp:
  clients:
    github:
      name: github
      enabled: true
      transport: streamable_http
      url: https://api.github.com/mcp
      headers:
        Authorization: Bearer ${GITHUB_TOKEN}
```

### 7.3 验证

```python
# 获取 MCP 客户端
client = await mcp_manager.get_client("filesystem")

# 列出可用工具
tools = await client.list_tools()

# 调用工具
result = await client.call_tool("read_file", {"path": "/workspace/test.txt"})
```

---

## 8. 设计意图

### 8.1 为什么用 MCP

| 优势 | 说明 |
|------|------|
| 开放标准 | 任何厂商可实现，不依赖特定云服务商 |
| 工具生态 | npm/PyPI 上已有数千个 MCP 服务器 |
| LLM 原生 | 专为 AI 模型设计，支持 prompts、资源、工具统一抽象 |
| 热重载 | 配置变更自动重连，无需重启服务 |

### 8.2 权衡取舍

| 设计决策 | 取 | 舍 |
|---------|----|----|
| 跨任务生命周期 | 避免 CancelScope 错误 | 增加代码复杂度 |
| 热重载 | 无停机更新 | 实现难度增加 |
| 多种传输模式 | 灵活适应不同场景 | 配置更复杂 |
| 锁外连接 | 高并发性能 | 短暂不一致窗口 |

### 8.3 与 Java SPI 对比

| Java SPI | Python MCP | 说明 |
|----------|------------|------|
| `ServiceLoader.load()` | `MCPClientManager` | 动态加载服务 |
| `META-INF/services/` | `config.yaml` mcp 配置 | 服务发现机制 |
| `XXService` 接口 | `StatefulClientBase` | 服务抽象基类 |
| 同步加载 | 异步 `asyncio` | 并发模型 |
| 静态配置 | 热更新 `MCPConfigWatcher` | 配置变更处理 |

---

## 9. 知识检查

1. **概念题**：解释 MCP 的作用，以及它与 Skill 的区别。

2. **分析题**：分析 `StdIOStatefulClient` 为什么需要将整个生命周期放在单一后台任务中运行？如果在 `connect()` 和 `close()` 中分别使用不同的 asyncio Task 会产生什么问题？

3. **配置题**：如何配置一个 MCP 服务器，包括 stdio 和 HTTP 两种传输模式？

4. **分析题**：分析 `MCPClientManager` 的热重载机制，说明"锁外连接，锁内替换"策略的优势。

---

## 本章小结

| 要点 | 内容 |
|------|------|
| **MCP 协议** | 开放标准，LLM 与外部工具交互 |
| **MCPClient** | `StdIOStatefulClient` + `HttpStatefulClient` |
| **管理器** | `MCPClientManager` 统一生命周期管理 |
| **热重载** | `MCPConfigWatcher` 轮询 + hash 快速跳过 |
| **传输模式** | stdio（本地）、streamable_http（远程） |
| **跨任务问题** | `_run_lifecycle()` 独立任务避免 CancelScope 错误 |

---

## 延伸阅读

- 源码：`src/qwenpaw/app/mcp/`
- MCP 官方文档：https://modelcontextprotocol.io/
- 官方服务器仓库：https://github.com/modelcontextprotocol/servers
- 智能体架构：第 2.1 章《Agent 核心架构》
- 工具系统：第 2.6 章《Guard 安全系统》
