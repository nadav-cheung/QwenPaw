# 第 2.1 章 Agent 核心架构

## 本章导览

| 项目 | 内容 |
|------|------|
| **学习目标** | 1) 理解 QwenPawAgent 的类继承层次结构 2) 掌握初始化流程的 6 个关键步骤 3) 分析 ReAct 推理-执行循环的源码实现 4) 了解工具系统的注册机制 5) 理解钩子系统的元类包装原理 |
| **前置知识** | 第 1.4 章 |
| **预计时长** | 60 分钟 |
| **难度等级** | ⭐⭐⭐ |
| **核心关键词** | `QwenPawAgent` `ReActAgent` `Toolkit` `Memory` `ModelFactory` `Mixin` |

> **本章概述**：深入分析 QwenPawAgent 核心架构的源码实现，揭示类继承设计、初始化流程、ReAct 循环机制及钩子系统的底层原理。

---

## 1. 类层次结构

### 1.1 继承链

```
QwenPawAgent → ToolGuardMixin → ReActAgent → Agent
```

**源码路径**: `src/qwenpaw/agents/react_agent.py:76`

```python
class QwenPawAgent(ToolGuardMixin, ReActAgent):
    """QwenPaw 智能体核心类，MRO 顺序决定方法解析"""

    # MRO note: ToolGuardMixin 覆盖 _acting 和 _reasoning
    # 子类若重写这两个方法，必须调用 super() 以保持安全拦截生效
```

**方法解析顺序（MRO）**：

| 优先级 | 类 | 主要职责 |
|--------|-----|---------|
| 1 | QwenPawAgent | 媒体过滤、命令处理、记忆管理 |
| 2 | ToolGuardMixin | 工具安全拦截 |
| 3 | ReActAgent | ReAct 推理-执行循环 |
| 4 | Agent | 基础智能体抽象 |

### 1.2 Mixin 模式

Python 多继承中，Mixin 是一种"混入"类，用于：

- **不依赖继承层次**即可横向扩展功能
- **方法冲突由 MRO 决定**谁优先
- **保持类职责单一**，便于组合

```python
# ToolGuardMixin 示例
class ToolGuardMixin:
    async def _acting(self, ...):  # 拦截工具调用
        # 安全检查逻辑
        return await super()._acting(...)  # 放行到 ReActAgent

class QwenPawAgent(ToolGuardMixin, ReActAgent):
    pass  # MRO: QwenPawAgent → ToolGuardMixin → ReActAgent → Agent
```

**关键设计**：Mixin 只扩展行为，不改变接口契约。调用方无需知道内部有多少层 Mixin。

---

## 2. 初始化流程详解

### 2.1 完整流程

```
__init__ (第 96-188 行)
    │
    ├── 1. 配置提取 (第 128-138 行)
    │       ↓
    ├── 2. 创建工具包 _create_toolkit (第 141 行)
    │       ↓
    ├── 3. 注册技能 _register_skills (第 144 行)
    │       ↓
    ├── 4. 构建系统提示词 _build_sys_prompt (第 147 行)
    │       ↓
    ├── 5. 创建模型 ModelFactory (第 150 行)
    │       ↓
    ├── 6. 初始化父类 ReActAgent (第 162-170 行)
    │       ↓
    ├── 7. 设置记忆管理器 _setup_memory_manager (第 173-177 行)
    │       ↓
    ├── 8. 注册命令处理器 CommandHandler (第 180-185 行)
    │       ↓
    └── 9. 注册钩子 _register_hooks (第 188 行)
```

### 2.2 源码分析

**源码路径**: `src/qwenpaw/agents/react_agent.py:96-188`

```python
def __init__(self, agent_config, env_context=None,
             enable_memory_manager=True, mcp_clients=None,
             memory_manager=None, request_context=None,
             namesake_strategy="skip", workspace_dir=None, ...):

    # 1. 配置提取
    self._agent_config = agent_config
    self._env_context = env_context
    self._request_context = dict(request_context or {})
    self._language = agent_config.language

    # 2. 创建工具包（注册内置工具）
    toolkit = self._create_toolkit(namesake_strategy=namesake_strategy)

    # 3. 加载技能
    self._register_skills(toolkit)

    # 4. 构建系统提示词
    sys_prompt = self._build_sys_prompt()

    # 5. 创建模型
    model, formatter = create_model_and_formatter(agent_id=agent_config.id)

    # 6. 初始化父类 ReActAgent
    super().__init__(
        name="Friday",
        model=model,
        sys_prompt=sys_prompt,
        toolkit=toolkit,
        memory=InMemoryMemory(),
        formatter=formatter,
        max_iters=running_config.max_iters,
    )

    # 7. 设置记忆管理器
    self._setup_memory_manager(enable_memory_manager, memory_manager, ...)

    # 8. 注册命令处理器
    self.command_handler = CommandHandler(...)

    # 9. 注册钩子
    self._register_hooks()
```

---

## 3. ReAct 推理-执行循环

### 3.1 推理阶段

**源码路径**: `src/qwenpaw/agents/react_agent.py:796-874`

```python
async def _reasoning(
    self,
    tool_choice: Literal["auto", "none", "required"] | None = None,
) -> Msg:
    """推理阶段：让模型决定下一步行动"""

    # 第一层：主动媒体过滤（模型不支持多模态时）
    if not get_active_model_supports_multimodal():
        n = self._proactive_strip_media_blocks()
        if n > 0:
            logger.warning(f"Proactively stripped {n} media block(s)")

    # 第二层：被动重试（模型调用失败时）
    try:
        msg = await super()._reasoning(tool_choice=tool_choice)
    except Exception as e:
        if not self._is_bad_request_or_media_error(e):
            raise
        # 媒体错误 → 剥离媒体块重试
        n_stripped = self._strip_media_blocks_from_memory()
        msg = await super()._reasoning(tool_choice=tool_choice)

    # 第三层：自动续期（模型返回纯文本时催促工具调用）
    return await self._auto_continue_if_text_only(msg, tool_choice)
```

**三层保护机制**：

1. **主动过滤**：模型不支持多模态时，预剥离媒体块
2. **被动重试**：模型拒绝媒体时，剥离后重试
3. **自动续期**：模型该用工具却返回文本时，注入提示催促

### 3.2 执行阶段

执行阶段由 `ReActAgent._acting` 实现，QwenPawAgent 通过 `ToolGuardMixin` 拦截：

```python
# ToolGuardMixin._acting 伪代码
async def _acting(self, reasoning_result):
    # 1. 安全检查：验证工具调用是否合法
    if not self._guard_check(reasoning_result):
        return self._blocked_response()

    # 2. 调用父类执行
    return await super()._acting(reasoning_result)
```

### 3.3 协作流程图

```
用户消息
    ↓
reply() → _reasoning() → 模型推理 → 决策（用工具/纯文本）
    ↓                              ↓
_print()                    _acting()
    ↓                         ↓
返回响应              工具执行 → 工具结果
                            ↓
                     返回 _reasoning()
```

---

## 4. 工具系统

### 4.1 Toolkit 创建

**源码路径**: `src/qwenpaw/agents/react_agent.py:190-307`

```python
def _create_toolkit(self, namesake_strategy="skip") -> Toolkit:
    toolkit = Toolkit()

    # 内置工具映射
    tool_functions = {
        "execute_shell_command": execute_shell_command,
        "read_file": read_file,
        "write_file": write_file,
        "edit_file": edit_file,
        "grep_search": grep_search,
        "glob_search": glob_search,
        "browser_use": browser_use,
        "desktop_screenshot": desktop_screenshot,
        "view_image": view_image,
        "view_video": view_video,
        "send_file_to_user": send_file_to_user,
        "get_current_time": get_current_time,
        "set_user_timezone": set_user_timezone,
        "get_token_usage": get_token_usage,
        "delegate_external_agent": delegate_external_agent,
        "list_agents": list_agents,
        "chat_with_agent": chat_with_agent,
        "submit_to_agent": submit_to_agent,
        "check_agent_task": check_agent_task,
    }

    # 按配置注册工具
    for tool_name, tool_func in tool_functions.items():
        if not enabled_tools.get(tool_name, True):  # 默认启用
            continue
        toolkit.register_tool_function(
            tool_func,
            namesake_strategy=namesake_strategy,
            async_execution=async_exec,
        )

    return toolkit
```

### 4.2 工具注册

**注册策略**（同名工具冲突时）：

| 策略 | 行为 |
|------|------|
| `override` | 用新工具覆盖已有 |
| `skip` | 保留已有（默认） |
| `raise` | 抛出异常 |
| `rename` | 重命名新工具 |

**异步执行**：只有 `execute_shell_command` 支持 `async_execution=True`，允许后台任务。

---

## 5. 钩子系统

### 5.1 钩子类型

| 钩子类型 | 触发时机 | 源码路径 |
|---------|---------|---------|
| BootstrapHook | 首次用户交互时检测 BOOTSTRAP.md | `src/qwenpaw/agents/hooks/bootstrap.py` |
| MemoryCompactionHook | 推理前检测上下文满载 | `src/qwenpaw/agents/hooks/memory_compaction.py` |

### 5.2 钩子注册

**源码路径**: `src/qwenpaw/agents/react_agent.py:428-456`

```python
def _register_hooks(self) -> None:
    # Bootstrap 钩子
    bootstrap_hook = BootstrapHook(
        working_dir=self._workspace_dir or WORKING_DIR,
        language=self._language,
    )
    self.register_instance_hook(
        hook_type="pre_reasoning",
        hook_name="bootstrap_hook",
        hook=bootstrap_hook.__call__,
    )

    # 记忆压缩钩子
    if self._enable_memory_manager and self.memory_manager is not None:
        memory_compact_hook = MemoryCompactionHook(
            memory_manager=self.memory_manager,
        )
        self.register_instance_hook(
            hook_type="pre_reasoning",
            hook_name="memory_compact_hook",
            hook=memory_compact_hook.__call__,
        )
```

### 5.3 元类包装机制

AgentScope 使用 `_ReActAgentMeta` 元类自动包装 `_reasoning` 和 `_acting`：

```python
# agentscope/agent/_agent_meta.py
class _ReActAgentMeta(_AgentMeta):
    def __new__(mcs, name, bases, attrs):
        # 为每个 _reasoning/_acting 方法添加钩子包装
        for func_name in ["_reasoning", "_acting"]:
            if func_name in attrs:
                attrs[func_name] = _wrap_with_hooks(attrs[func_name])
        return super().__new__(mcs, name, bases, attrs)
```

**重入保护**：防止多层包装导致钩子重复执行：

```python
# 元类包装内部
hook_guard_attr = f"_hook_running_{func_name}"

async def async_wrapper(self, *args, **kwargs):
    # 已在执行中？跳过钩子直接执行原函数
    if getattr(self, hook_guard_attr, False):
        return await original_func(self, *args, **kwargs)
    # ...
```

### 5.4 钩子执行流程

```
reply()
    ↓
_reasoning() ← pre_reasoning hooks 触发
    ├── BootstrapHook: 检测 BOOTSTRAP.md
    └── MemoryCompactionHook: 检测上下文满载
    ↓
_acting() ← pre_acting hooks 触发
    ↓
工具执行
    ↓
post_acting hooks → post_reasoning hooks
    ↓
reply() 返回
```

---

## 6. 设计意图

### 6.1 为什么用 Mixin

**优势**：

| 方面 | 说明 |
|------|------|
| **组合优于继承** | ToolGuardMixin 可与任意 Agent 基类组合 |
| **单一职责** | 每个 Mixin 只负责一件事（安全拦截、媒体过滤） |
| **MRO 解耦** | 子类无需知道各 Mixin 的加载顺序 |

**权衡**：

- MRO 复杂时难以追踪，建议用 `ClassName.__mro__` 调试
- Mixin 不应依赖具体子类，只通过父类接口通信

### 6.2 权衡取舍

| 设计决策 | 取 | 舍 |
|---------|----|----|
| 多继承 Mixin | 灵活组合功能 | MRO 复杂度 |
| 钩子系统 | 无侵入扩展 | 重入保护负担 |
| 媒体三重保护 | 兼容多种模型 | 增加调用延迟 |
| 命令处理器 | 用户体验好 | 增加初始化复杂度 |

---

## 7. 知识检查

1. **分析题**：画出 QwenPawAgent 的完整继承链，并标注每个类的职责。

2. **源码题**：分析 `_reasoning` 方法的三层保护机制，说明每层的作用。

3. **设计题**：解释 ToolGuardMixin 的作用，为什幺它能拦截 `_acting` 方法。

4. **实战题**：如果需要在工具执行前添加日志，如何实现自定义钩子？

---

## 本章小结

| 要点 | 内容 |
|------|------|
| **核心类** | `QwenPawAgent(ToolGuardMixin, ReActAgent)` |
| **MRO** | QwenPawAgent → ToolGuardMixin → ReActAgent → Agent |
| **初始化** | 9 步流程 |
| **ReAct 循环** | `_reasoning()` + `_acting()` + 三层媒体保护 |
| **工具注册** | `Toolkit.register_tool_function()` + 4 种命名策略 |
| **钩子** | BootstrapHook + MemoryCompactionHook + 元类自动包装 |

---

## 延伸阅读

- 源码：`src/qwenpaw/agents/react_agent.py`
- 钩子实现：`src/qwenpaw/agents/hooks/`
- 安全系统：第 2.6 章《Guard 安全系统》
- 记忆系统：第 2.3 章《记忆系统》
