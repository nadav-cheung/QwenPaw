# 第 2.4 章 Provider 与模型层

## 本章导览

| 项目 | 内容 |
|------|------|
| **学习目标** | 1) 理解 Provider 抽象层的架构设计与核心组件 2) 掌握 ModelFactory 的工厂模式和模型包装管道 3) 理解模型探测与能力检测机制 |
| **前置知识** | 第 1.4 章 |
| **预计时长** | 60 分钟 |
| **难度等级** | ⭐⭐⭐ |
| **核心关键词** | `Provider` `ModelFactory` `LLM` `本地模型` |

> **本章概述**：深入分析 Provider 抽象层和模型配置，涵盖 ProviderManager 单例管理、模型工厂、RetryChatModel 重试限流机制，以及模型能力探测系统。

---

## 1. Provider 系统概述

### 1.1 为什么需要 Provider

Provider 抽象层让 QwenPaw 能够支持多种 LLM 提供商，通过统一接口封装不同模型 API 的差异。系统内置 24+ Provider，支持模型发现、连接检查、多模态探测和密钥加密存储。

**核心职责**：
- 统一封装不同模型 API 的差异
- 提供模型发现、连接检查、多模态探测能力
- 管理 API 密钥加密存储
- 支持 Provider 热切换和自定义 Provider 扩展

### 1.2 支持的提供商

| 提供商 | 模型示例 | 配置 Key | 类型 |
|--------|---------|---------|------|
| OpenAI | GPT-4, GPT-3.5 | openai | 云端 |
| Anthropic | Claude 3 | anthropic | 云端 |
| Google Gemini | Gemini 2.0 | gemini | 云端 |
| DeepSeek | DeepSeek-V3 | deepseek | 云端 |
| 阿里云 DashScope | Qwen3-max | dashscope | 云端 |
| 智谱 AI | GLM-5 | zhipu-cn | 云端 |
| Ollama | Llama, Qwen | ollama | 本地 |
| LM Studio | 本地模型 | lmstudio | 本地 |
| QwenPaw Local | 内置 llama.cpp | qwenpaw-local | 本地 |

---

## 2. Provider 架构

### 2.1 抽象基类

**源码路径**: `src/qwenpaw/providers/provider.py:137`

```python
class Provider(ProviderInfo, ABC):
    """Provider 抽象基类"""

    @abstractmethod
    async def check_connection(self, timeout: float = 5) -> tuple[bool, str]:
        """检查 Provider 是否可达"""

    @abstractmethod
    async def fetch_models(self, timeout: float = 5) -> List[ModelInfo]:
        """获取 Provider 支持的模型列表"""

    @abstractmethod
    async def check_model_connection(
        self,
        model_id: str,
        timeout: float = 5,
    ) -> tuple[bool, str]:
        """检查特定模型是否可用"""

    @abstractmethod
    def get_chat_model_instance(self, model_id: str) -> ChatModelBase:
        """获取聊天模型实例"""
```

### 2.2 核心数据类

**ModelInfo** (`provider.py:22`):

```python
class ModelInfo(BaseModel):
    id: str                           # API 调用使用的模型标识符
    name: str                         # 人类可读的模型名称
    supports_multimodal: bool | None  # 是否支持多模态输入
    supports_image: bool | None       # 是否支持图像输入
    supports_video: bool | None       # 是否支持视频输入
    probe_source: str | None          # 'documentation' 或 'probed'
    is_free: bool = False             # 是否免费使用
    generate_kwargs: Dict[str, Any] = {}  # 模型级生成参数覆盖
```

**ProviderInfo** (`provider.py:70`):

```python
class ProviderInfo(BaseModel):
    id: str                           # Provider 标识符
    name: str                         # 人类可读的 Provider 名称
    base_url: str = ""                # API 基础 URL
    api_key: str = ""                 # API 密钥（加密存储）
    chat_model: str = "OpenAIChatModel"  # ChatModel 名称
    models: List[ModelInfo] = []       # 预定义模型列表
    extra_models: List[ModelInfo] = []  # 用户添加的模型列表
    is_local: bool = False            # 是否为本地托管平台
    freeze_url: bool = False          # base_url 是否冻结
    require_api_key: bool = True      # 是否需要 API 密钥
    is_custom: bool = False           # 是否为用户创建
    support_model_discovery: bool = False  # 是否支持模型列表发现
```

### 2.3 ProviderManager 单例

**源码路径**: `src/qwenpaw/providers/provider_manager.py:720`

```python
class ProviderManager:
    """单例模式的提供商管理器"""

    _instance: Optional["ProviderManager"] = None

    def __init__(self) -> None:
        self.builtin_providers: Dict[str, Provider] = {}
        self.custom_providers: Dict[str, Provider] = {}
        self.plugin_providers: Dict[str, Dict] = {}
        self.active_model: ModelSlotConfig | None = None
        self._prepare_disk_storage()
        self._init_builtins()

    @staticmethod
    def get_instance() -> "ProviderManager":
        if ProviderManager._instance is None:
            ProviderManager._instance = ProviderManager()
        return ProviderManager._instance
```

**核心方法**:

| 方法 | 行号 | 说明 |
|------|------|------|
| `get_provider(provider_id)` | 789 | 获取 Provider 实例 |
| `activate_model(provider_id, model_id)` | 985 | 激活指定模型 |
| `fetch_provider_models(provider_id, save)` | 900 | 获取 Provider 支持的模型列表 |
| `probe_model_multimodal(provider_id, model_id)` | 1124 | 探测模型多模态能力 |

---

## 3. ModelFactory

### 3.1 模型工厂

**源码路径**: `src/qwenpaw/agents/model_factory.py:930`

```python
def create_model_and_formatter(
    agent_id: Optional[str] = None,
) -> Tuple[ChatModelBase, FormatterBase]:
    """创建模型和格式化器实例

    流程:
    1. 解析 agent_id (参数 > context > None)
    2. 加载代理配置 (RetryConfig, RateLimitConfig)
    3. 获取 provider 和 model
    4. 创建格式化器
    5. 双重包装模型: TokenRecordingModelWrapper → RetryChatModel
    """
```

### 3.2 模型包装管道

```
ProviderManager.get_active_chat_model()
         │
         ▼
   ┌───────────────────────┐
   │ OpenAIChatModelCompat │
   │ (或 Anthropic/Gemini) │
   └───────────────────────┘
         │ (包装)
         ▼
   ┌─────────────────────────────┐
   │ TokenRecordingModelWrapper │
   │ - 记录 token 使用量          │
   │ - 修复 vLLM tool_choice     │
   └─────────────────────────────┘
         │ (包装)
         ▼
   ┌─────────────────────┐
   │   RetryChatModel     │
   │ - 限流信号量         │
   │ - 指数退避重试       │
   └─────────────────────┘
```

### 3.3 模型配置

```yaml
model:
  provider: openai
  model: gpt-4
  api_key: xxx
  base_url: https://api.openai.com  # 可选
```

**配置模型** (`config/config.py`):

```python
class ModelSlotConfig(BaseModel):
    """单个模型槽位配置"""
    provider_id: str                          # 提供商 ID
    model: str                                # 模型名称
    api_key: Optional[str] = None             # API Key
    base_url: Optional[str] = None            # 自定义端点
    timeout: Optional[float] = None           # 请求超时
    max_retries: int = 3                     # 最大重试次数

class ProviderConfig(BaseModel):
    """提供商配置"""
    provider_id: str
    enabled: bool = True
    api_key: Optional[str] = None
    models: List[ModelSlotConfig] = []
```

---

## 4. 本地模型

### 4.1 Ollama

OllamaProvider 继承自 OpenAIProvider，复用 OpenAI 兼容 API。

**源码路径**: `src/qwenpaw/providers/ollama_provider.py`

```yaml
model:
  provider: ollama
  model: llama3
  base_url: http://localhost:11434
```

**URL 规范化机制**:

```python
@staticmethod
def _normalize_base_url(base_url: str) -> str:
    """规范化 base_url，去除末尾 /v1"""
    normalized_base_url = (base_url or "").rstrip("/")
    if normalized_base_url.endswith("/v1"):
        normalized_base_url = normalized_base_url[:-3].rstrip("/")
    return normalized_base_url

def _openai_compatible_base_url(self) -> str:
    """返回 OpenAI 兼容的 base_url，加上 /v1"""
    return self._normalize_base_url(self.base_url) + "/v1"
```

### 4.2 LM Studio

```yaml
model:
  provider: lmstudio
  model: local
  base_url: http://localhost:1234
```

### 4.3 QwenPaw 内置 llama.cpp

**源码路径**: `src/qwenpaw/local_models/manager.py:41`

```python
class LocalModelManager:
    """本地模型管理器 Facade"""

    def setup_server(model_id: str) -> StartServerResponse: ...  # 启动服务
    def shutdown_server(): ...                                    # 关闭服务
```

**启动流程**:
1. 检查 llama-server 可执行文件存在
2. 验证模型路径有效
3. 若相同模型已在运行，直接返回
4. 若不同模型运行，先关闭旧服务
5. 创建子进程: `llama-server --host 127.0.0.1 --port <port> --model <path>`
6. 轮询 /health 端点等待就绪（最多 120s）

---

## 5. RetryChatModel 重试与限流

### 5.1 配置参数

**源码路径**: `src/qwenpaw/providers/retry_chat_model.py`

```python
LLM_MAX_CONCURRENT = 10    # 最大并发调用数
LLM_MAX_QPM = 600          # 每分钟最大请求数
LLM_RATE_LIMIT_PAUSE = 5.0 # 限流暂停时间（秒）
LLM_ACQUIRE_TIMEOUT = 300.0 # 获取信号量超时（秒）
```

### 5.2 重试配置

```python
@dataclass(frozen=True, slots=True)
class RetryConfig:
    """重试策略配置"""
    enabled: bool = True
    max_retries: int = 3
    backoff_base: float = 1.0  # 初始退避时间（秒）
    backoff_cap: float = 10.0  # 最大退避时间

@dataclass(frozen=True, slots=True)
class RateLimitConfig:
    """速率限制配置"""
    max_concurrent: int = 10
    max_qpm: int = 600
    pause_seconds: float = 5.0
    jitter_range: float = 1.0
    acquire_timeout: float = 300.0
```

### 5.3 重试机制

**可重试错误码**: `{429, 500, 502, 503, 504, 529}`

**重试流程**:
```
chat() 调用
         │
         ▼
获取信号量 (max_concurrent)
         │
         ▼
try: 执行 chat
         │
         ├── 成功 → yield chunks → 释放信号量
         │
         └── 失败
                 │
                 ├── 不可重试 → 释放信号量 → 抛出异常
                 │
                 └── 可重试
                         │
                         ├── 还有重试次数 → 计算延迟 → sleep → 重试
                         │
                         └── 没有重试次数 → 释放信号量 → 抛出异常
```

**流式响应槽位管理**: 信号量槽位在首个 chunk 到达后释放，避免流式响应期间占用槽位导致其他调用者饥饿。

---

## 6. 模型探测

### 6.1 能力检测

**源码路径**: `src/qwenpaw/providers/multimodal_prober.py`

```python
class MultimodalProber:
    """检测模型多模态能力"""

    async def probe(self, provider: Provider, model_id: str) -> ProbeResult:
        """探测模型支持的功能"""
        # 图像探测
        image_result = await self._probe_image(provider, model_id)
        # 视频探测
        video_result = await self._probe_video(provider, model_id)
        return ProbeResult(
            supports_image=image_result,
            supports_video=video_result,
        )
```

### 6.2 探测数据

**探测图像**: 32x32 红色 PNG (96 bytes)
**探测视频**: 64x64 蓝色 H.264 MP4 (1.8KB)

**探测策略**: 使用颜色识别作为判断依据，颜色是视觉任务的最简单形式，正确答案明确（红色），易于验证。

### 6.3 支持的能力

| 能力 | 说明 | 探测方式 |
|------|------|----------|
| vision | 支持图片输入 | 发送红色图像，检测是否识别颜色 |
| function_calling | 支持函数调用 | 检查 API 兼容性 |
| streaming | 支持流式输出 | 检查 stream 参数支持 |

### 6.4 探测结果缓存

**probe_source 字段含义**:

| 值 | 含义 | 说明 |
|-----|------|------|
| `documentation` | 文档标注 | 来自官方文档或内置配置 |
| `probed` | 已探测 | 已通过实际 API 调用验证 |
| `unknown` | 未知 | 尚未确定能力 |

---

## 7. 设计意图

### 7.1 Provider 模式优势

1. **统一接口**: 不同的 LLM API 有不同的接口设计，Provider 抽象层提供统一接口
2. **可替换性**: 可以在运行时切换不同的模型提供商
3. **可扩展性**: 通过继承 BaseProvider 可以添加新的提供商
4. **关注分离**: 调用方不需要关心底层模型的具体实现

### 7.2 权衡取舍

| 方面 | 云端模型 | 本地模型 |
|------|----------|----------|
| 成本 | 按 token 计费 | 一次性硬件投入 |
| 性能 | 强大算力，低延迟 | 依赖本地硬件 |
| 隐私 | 数据离开本地 | 数据不出本地 |
| 可用性 | 需要网络连接 | 可离线使用 |
| 灵活性 | 随时切换模型 | 受硬件限制 |

### 7.3 工厂模式与包装器

**为什么需要多层包装**:
- `TokenRecordingModelWrapper`: 记录 token 使用量，用于计费和监控
- `RetryChatModel`: 处理瞬态错误和限流，保证可靠性

包装顺序很重要：RetryChatModel 在最外层，确保所有调用都经过重试逻辑。

---

## 8. 知识检查

1. **概念题**：解释 Provider 抽象层的作用。为什么需要 Provider 而不是直接调用模型 API？

2. **配置题**：如何配置使用 Ollama 本地模型？请给出完整的配置示例。

3. **分析题**：分析 ModelFactory 的双重包装机制。TokenRecordingModelWrapper 和 RetryChatModel 各自承担什么职责？为什么 RetryChatModel 是最外层？

4. **理解题**：解释模型探测的流程。probe_source 字段的三个值（documentation/probed/unknown）分别代表什么含义？

---

## 本章小结

| 要点 | 内容 |
|------|------|
| Provider | BaseProvider 抽象，24+ 内置提供商 |
| ProviderManager | 单例模式管理所有 Provider |
| 工厂模式 | ModelFactory 创建和缓存模型实例 |
| 本地模型 | Ollama, LM Studio, llama.cpp |
| 重试限流 | RetryChatModel 指数退避 + 信号量 |
| 能力探测 | MultimodalProber 零配置判断多模态 |

---

## 延伸阅读

- 源码：`src/qwenpaw/providers/`
- Provider 基类：`src/qwenpaw/providers/provider.py`
- ProviderManager：`src/qwenpaw/providers/provider_manager.py`
- 模型工厂：`src/qwenpaw/agents/model_factory.py`
- 重试包装器：`src/qwenpaw/providers/retry_chat_model.py`
- 多模态探测：`src/qwenpaw/providers/multimodal_prober.py`
- 本地模型：`src/qwenpaw/local_models/`
- 配置项索引：附录 B《配置项索引》
