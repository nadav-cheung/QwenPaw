# 第 3.2 章 智能客服系统

## 项目概述

### 项目目标
创建一个智能客服系统，实现：
- 多渠道统一接入
- 意图识别和 FAQ 匹配
- 转人工和工单系统

### 技术架构

```
多渠道(Channel) → 消息路由 → 意图识别(Agent) → FAQ/Memory → 响应
```

### 技术栈

| 组件 | 用途 | 涉及章节 |
|------|------|---------|
| QwenPaw Agent | 核心智能体 | 2.1 |
| ChannelManager | 多渠道管理 | 2.5 |
| Memory | FAQ 知识库 | 2.3 |
| Skill | 意图识别 | 2.7 |
| Approval | 转人工审批 | 2.6 |

---

## 实现步骤

### 步骤 1：FAQ 知识库

#### 1.1 FAQ 数据结构

**文件**: `faq_kb.py`

```python
"""FAQ 知识库"""

from dataclasses import dataclass
from typing import Optional

@dataclass
class FAQ:
    """FAQ 条目"""
    id: str
    question: str
    answer: str
    keywords: list[str]
    category: str
    confidence_threshold: float = 0.7

class FAQKnowledgeBase:
    """FAQ 知识库"""
    
    def __init__(self):
        self.faqs: list[FAQ] = []
    
    def add(self, faq: FAQ):
        self.faqs.append(faq)
    
    def search(self, query: str) -> list[tuple[FAQ, float]]:
        """搜索最匹配的 FAQ，返回 (faq, score) 列表"""
        scores = []
        query_lower = query.lower()
        
        for faq in self.faqs:
            # 简单关键词匹配
            score = sum(1 for kw in faq.keywords if kw.lower() in query_lower)
            score += sum(0.5 for kw in faq.keywords if kw.lower() in faq.question.lower())
            
            if score > 0:
                scores.append((faq, score / len(faq.keywords)))
        
        # 按分数排序
        scores.sort(key=lambda x: x[1], reverse=True)
        return scores
    
    def load_from_file(self, filepath: str):
        """从文件加载 FAQ"""
        import json
        with open(filepath, "r", encoding="utf-8") as f:
            data = json.load(f)
            for item in data:
                self.add(FAQ(**item))
```

#### 1.2 示例 FAQ 数据

**文件**: `data/faqs.json`

```json
[
  {
    "id": "faq_001",
    "question": "如何重置密码",
    "answer": "请访问 https://example.com/reset-password 进行密码重置",
    "keywords": ["密码", "重置", "忘记密码", "reset password"],
    "category": "账户",
    "confidence_threshold": 0.7
  },
  {
    "id": "faq_002",
    "question": "如何联系人工客服",
    "answer": "请联系 support@example.com 或拨打 400-xxx-xxxx",
    "keywords": ["人工", "客服", "联系", "support"],
    "category": "服务",
    "confidence_threshold": 0.8
  },
  {
    "id": "faq_003",
    "question": "如何修改个人信息",
    "answer": "请登录后进入「个人中心」→「设置」修改您的个人信息",
    "keywords": ["修改", "个人信息", "资料", "设置"],
    "category": "账户",
    "confidence_threshold": 0.7
  },
  {
    "id": "faq_004",
    "question": "如何绑定手机号",
    "answer": "请在「个人中心」→「安全设置」中绑定手机号",
    "keywords": ["绑定", "手机", "手机号", "验证"],
    "category": "账户",
    "confidence_threshold": 0.7
  },
  {
    "id": "faq_005",
    "question": "如何查看订单状态",
    "answer": "请在「我的订单」页面查看订单状态，如有疑问请联系客服",
    "keywords": ["订单", "状态", "查看", "物流"],
    "category": "订单",
    "confidence_threshold": 0.7
  },
  {
    "id": "faq_006",
    "question": "如何申请退款",
    "answer": "请在「订单详情」页面点击「申请退款」，填写原因后提交",
    "keywords": ["退款", "退货", "取消订单", "refund"],
    "category": "订单",
    "confidence_threshold": 0.7
  },
  {
    "id": "faq_007",
    "question": "如何开通会员",
    "answer": "请访问 https://example.com/vip 了解会员权益并开通",
    "keywords": ["会员", "VIP", "开通", "订阅"],
    "category": "付费",
    "confidence_threshold": 0.7
  },
  {
    "id": "faq_008",
    "question": "如何获取发票",
    "answer": "请在「个人中心」→「订单管理」中申请开具发票",
    "keywords": ["发票", "开票", "报销"],
    "category": "财务",
    "confidence_threshold": 0.7
  }
]
```

### 步骤 2：创建客服智能体

#### 2.1 配置文件

**文件**: `config/customer_service.yaml`

```yaml
agent:
  name: customer_service
  model:
    provider: openai
    model: gpt-4
  
skills:
  - name: faq_match
    enabled: true
  - name: intent_classify
    enabled: true

memory:
  backend: faq
  max_tokens: 2000

channels:
  - type: dingtalk
    app_key: ${DINGTALK_APP_KEY}
    app_secret: ${DINGTALK_APP_SECRET}
  - type: feishu
    app_id: ${FEISHU_APP_ID}
    app_secret: ${FEISHU_APP_SECRET}
  - type: discord
    token: ${DISCORD_BOT_TOKEN}
```

#### 2.2 客服智能体代码

**文件**: `customer_service.py`

```python
"""智能客服系统"""

from dataclasses import dataclass
from typing import Optional
from datetime import datetime
from qwenpaw import QwenPawAgent
from faq_kb import FAQKnowledgeBase, FAQ

@dataclass
class CustomerQuery:
    """用户查询"""
    content: str
    channel: str
    user_id: str
    timestamp: str

class CustomerServiceAgent:
    """客服智能体"""
    
    def __init__(self, config_path: str, faq_file: str):
        self.agent = QwenPawAgent(config=config_path)
        self.faq_kb = FAQKnowledgeBase()
        self.faq_kb.load_from_file(faq_file)
        
        # 转人工阈值
        self.human_threshold = 0.3
        
        # 意图分类提示
        self.intent_prompt = """请判断用户意图，只回答一个词：
- faq: 常见问题
- human: 需要人工客服
- other: 其他

用户消息：{message}
意图："""
    
    async def classify_intent(self, message: str) -> str:
        """意图分类"""
        response = await self.agent.run(
            self.intent_prompt.format(message=message)
        )
        response = response.strip().lower()
        
        if "faq" in response:
            return "faq"
        elif "human" in response:
            return "human"
        return "other"
    
    async def find_faq(self, message: str) -> Optional[tuple[FAQ, float]]:
        """查找匹配的 FAQ"""
        matches = self.faq_kb.search(message)
        
        if not matches:
            return None
        
        best_faq, best_score = matches[0]
        
        # 如果置信度低于阈值，转人工
        if best_score < self.human_threshold:
            return None
        
        return (best_faq, best_score)
    
    async def handle_query(self, query: CustomerQuery) -> str:
        """处理用户查询"""
        # 1. 意图分类
        intent = await self.classify_intent(query.content)
        
        if intent == "faq":
            # 2. 查找 FAQ
            result = await self.find_faq(query.content)
            
            if result:
                faq, score = result
                return f"{faq.answer}\n\n💡 这是 {faq.category} 类问题吗？"
            
            # FAQ 置信度不够，转人工
            return await self.transfer_to_human(query, reason="faq_low_confidence")
        
        elif intent == "human":
            return await self.transfer_to_human(query, reason="user_requested")
        
        # 3. 其他问题，用 AI 回答
        response = await self.agent.run(query.content)
        
        # 添加建议
        response += "\n\n❓ 还有其他问题吗？回复「人工」转接客服。"
        
        return response
    
    async def transfer_to_human(self, query: CustomerQuery, reason: str) -> str:
        """转人工客服"""
        # 发送通知给客服
        notification = f"""🔔 新客服请求
渠道：{query.channel}
用户：{query.user_id}
原因：{reason}
问题：{query.content}"""
        
        await self.agent.send_to_channel("internal", notification)
        
        return "请稍等，正在为您转接人工客服..."
    
    async def process(self, message: str, channel: str, user_id: str) -> str:
        """处理消息"""
        query = CustomerQuery(
            content=message,
            channel=channel,
            user_id=user_id,
            timestamp=datetime.now().isoformat()
        )
        
        return await self.handle_query(query)
```

### 步骤 3：多渠道适配

#### 3.1 渠道消息转换

**文件**: `channel_adapter.py`

```python
"""渠道适配器"""

from abc import ABC, abstractmethod
from typing import Any

class ChannelAdapter(ABC):
    """渠道适配器基类"""
    
    @abstractmethod
    def extract_text(self, raw_message: Any) -> str:
        """从渠道消息中提取文本"""
        pass
    
    @abstractmethod
    def format_response(self, text: str) -> Any:
        """格式化响应"""
        pass
    
    @abstractmethod
    def extract_user_id(self, raw_message: Any) -> str:
        """从渠道消息中提取用户 ID"""
        pass

class DingTalkAdapter(ChannelAdapter):
    """钉钉适配器"""
    
    def extract_text(self, raw_message: dict) -> str:
        # 钉钉消息格式
        return raw_message.get("text", {}).get("content", "")
    
    def format_response(self, text: str) -> dict:
        return {
            "msgtype": "text",
            "text": {"content": text}
        }
    
    def extract_user_id(self, raw_message: dict) -> str:
        return raw_message.get("sender_nick", "unknown")

class FeishuAdapter(ChannelAdapter):
    """飞书适配器"""
    
    def extract_text(self, raw_message: dict) -> str:
        # 飞书消息格式
        return raw_message.get("text", {}).get("content", "")
    
    def format_response(self, text: str) -> dict:
        return {
            "msg_type": "text",
            "content": {"text": text}
        }
    
    def extract_user_id(self, raw_message: dict) -> str:
        return raw_message.get("sender", {}).get("sender_id", {}).get("open_id", "unknown")

class DiscordAdapter(ChannelAdapter):
    """Discord 适配器"""
    
    def extract_text(self, raw_message: dict) -> str:
        # Discord 消息格式
        return raw_message.get("content", "")
    
    def format_response(self, text: str) -> dict:
        return {"content": text}
    
    def extract_user_id(self, raw_message: dict) -> str:
        return raw_message.get("author", {}).get("id", "unknown")
```

### 步骤 4：主程序

**文件**: `main.py`

```python
"""智能客服入口"""

import asyncio
from datetime import datetime
from customer_service import CustomerServiceAgent
from channel_adapter import DingTalkAdapter, FeishuAdapter, DiscordAdapter

# 初始化
agent = CustomerServiceAgent(
    config_path="config/customer_service.yaml",
    faq_file="data/faqs.json"
)

# 适配器映射
adapters = {
    "dingtalk": DingTalkAdapter(),
    "feishu": FeishuAdapter(),
    "discord": DiscordAdapter(),
}

async def handle_dingtalk_webhook(request: dict):
    """处理钉钉 webhook 请求"""
    adapter = adapters["dingtalk"]
    
    text = adapter.extract_text(request)
    user_id = adapter.extract_user_id(request)
    
    response_text = await agent.process(
        message=text,
        channel="dingtalk",
        user_id=user_id
    )
    
    return adapter.format_response(response_text)

async def handle_feishu_webhook(request: dict):
    """处理飞书 webhook 请求"""
    adapter = adapters["feishu"]
    
    text = adapter.extract_text(request)
    user_id = adapter.extract_user_id(request)
    
    response_text = await agent.process(
        message=text,
        channel="feishu",
        user_id=user_id
    )
    
    return adapter.format_response(response_text)

async def handle_discord_message(message: dict):
    """处理 Discord 消息"""
    adapter = adapters["discord"]
    
    text = adapter.extract_text(message)
    user_id = adapter.extract_user_id(message)
    
    response_text = await agent.process(
        message=text,
        channel="discord",
        user_id=user_id
    )
    
    return adapter.format_response(response_text)

# 测试
async def test():
    response = await agent.process(
        message="我想重置密码",
        channel="test",
        user_id="test_user"
    )
    print(response)

if __name__ == "__main__":
    asyncio.run(test())
```

---

## 完整代码

### 项目结构

```
customer_service/
├── config/
│   └── customer_service.yaml
├── customer_service.py
├── faq_kb.py
├── channel_adapter.py
├── main.py
└── data/
    └── faqs.json
```

### requirements.txt

```
qwenpaw>=1.1.0
fastapi>=0.100.0
uvicorn>=0.23.0
pyyaml>=6.0
```

---

## 运行与测试

### 启动服务

```bash
# 安装依赖
pip install -r requirements.txt

# 配置环境变量
export DINGTALK_APP_KEY=xxx
export DINGTALK_APP_SECRET=xxx
export FEISHU_APP_ID=xxx
export FEISHU_APP_SECRET=xxx

# 启动服务
uvicorn main:app --host 0.0.0.0 --port 8000
```

### 测试 FAQ 功能

```python
import asyncio
from customer_service import CustomerServiceAgent

async def test():
    agent = CustomerServiceAgent(
        config_path="config/customer_service.yaml",
        faq_file="data/faqs.json"
    )
    
    # 测试 FAQ
    response = await agent.process(
        message="密码忘了怎么办",
        channel="test",
        user_id="test"
    )
    print(f"Q: 密码忘了怎么办\nA: {response}")

asyncio.run(test())
```

### 完整测试用例

```python
import asyncio
from customer_service import CustomerServiceAgent

async def run_tests():
    agent = CustomerServiceAgent(
        config_path="config/customer_service.yaml",
        faq_file="data/faqs.json"
    )
    
    test_cases = [
        ("密码忘了怎么办", "账户类问题"),
        ("如何查看订单", "订单类问题"),
        ("要联系人工客服", "转人工"),
        ("你好，我想问一下产品问题", "AI 回答"),
    ]
    
    print("=" * 50)
    print("智能客服系统测试")
    print("=" * 50)
    
    for message, expected in test_cases:
        response = await agent.process(
            message=message,
            channel="test",
            user_id="test_user"
        )
        print(f"\n输入: {message}")
        print(f"预期: {expected}")
        print(f"输出: {response[:100]}..." if len(response) > 100 else f"输出: {response}")
        print("-" * 50)

if __name__ == "__main__":
    asyncio.run(run_tests())
```

### 预期输出

```
==================================================
智能客服系统测试
==================================================

输入: 密码忘了怎么办
预期: 账户类问题
输出: 请访问 https://example.com/reset-password 进行密码重置

💡 这是 账户 类问题吗？
--------------------------------------------------

输入: 如何查看订单
预期: 订单类问题
输出: 请在「我的订单」页面查看订单状态，如有疑问请联系客服

💡 这是 订单 类问题吗？
--------------------------------------------------

输入: 要联系人工客服
预期: 转人工
输出: 请稍等，正在为您转接人工客服...
--------------------------------------------------

输入: 你好，我想问一下产品问题
预期: AI 回答
输出: [AI 生成的回答]

❓ 还有其他问题吗？回复「人工」转接客服。
--------------------------------------------------
```

### FAQ 知识库单元测试

```python
import unittest
from faq_kb import FAQKnowledgeBase, FAQ

class TestFAQKnowledgeBase(unittest.TestCase):
    def setUp(self):
        self.kb = FAQKnowledgeBase()
        self.kb.add(FAQ(
            id="test_001",
            question="如何重置密码",
            answer="访问 reset 页",
            keywords=["密码", "重置"],
            category="账户"
        ))
    
    def test_search_exact_keyword(self):
        results = self.kb.search("密码")
        self.assertGreater(len(results), 0)
    
    def test_search_partial_match(self):
        results = self.kb.search("我想重置")
        self.assertGreater(len(results), 0)
    
    def test_search_no_match(self):
        results = self.kb.search("完全不相关的内容 xyz")
        self.assertEqual(len(results), 0)
    
    def test_empty_query(self):
        results = self.kb.search("")
        self.assertEqual(len(results), 0)

if __name__ == "__main__":
    unittest.main()
```

---

## 扩展功能

### 扩展 1：FAQ 满意度评价

```python
@dataclass
class Feedback:
    """用户反馈"""
    query_id: str
    rating: int  # 1-5
    comment: str
    created_at: datetime

class FeedbackManager:
    """反馈管理器"""
    
    def __init__(self):
        self.feedbacks: list[Feedback] = []
    
    async def handle_feedback(self, query_id: str, rating: int, comment: str):
        """处理用户反馈"""
        feedback = Feedback(
            query_id=query_id,
            rating=rating,
            comment=comment,
            created_at=datetime.now()
        )
        self.feedbacks.append(feedback)
        
        # 如果差评，通知人工
        if rating <= 2:
            await self.notify_human(f"用户对回答 {query_id} 评分 {rating}：{comment}")
    
    async def notify_human(self, message: str):
        """通知人工客服"""
        print(f"通知人工客服: {message}")
```

### 扩展 2：多轮对话

```python
async def handle_followup(self, query: CustomerQuery, context: dict) -> str:
    """处理多轮对话"""
    # 获取对话历史
    history = await self.agent.memory.get_recent(max_turns=5)
    
    # 追加当前问题
    history.append({"role": "user", "content": query.content})
    
    # 带上下文回答
    response = await self.agent.run_with_history(history)
    
    return response

async def handle_conversation_flow(self, query: CustomerQuery) -> str:
    """处理对话流程"""
    # 检查是否在对话中
    session_key = f"{query.channel}:{query.user_id}"
    
    if session_key in self.active_sessions:
        session = self.active_sessions[session_key]
        
        # 如果用户确认/否定
        if query.content in ["是", "是的", "对"]:
            session.acknowledged = True
            return "感谢您的确认，如有其他问题随时联系我们。"
        
        if query.content in ["否", "不是", "不对"]:
            session.acknowledged = False
            return await self.transfer_to_human(query, reason="user_correction")
    
    # 新对话
    return await self.handle_query(query)
```

### 扩展 3：工单系统

```python
@dataclass
class Ticket:
    """工单"""
    id: str
    user_id: str
    channel: str
    question: str
    status: str  # open, pending, resolved, closed
    assigned_to: Optional[str]
    created_at: datetime
    updated_at: datetime

class TicketSystem:
    """工单系统"""
    
    def __init__(self):
        self.tickets: dict[str, Ticket] = {}
    
    def generate_id(self) -> str:
        """生成工单 ID"""
        import uuid
        return f"TKT-{uuid.uuid4().hex[:8].upper()}"
    
    async def create_ticket(self, query: CustomerQuery, reason: str) -> Ticket:
        """创建工单"""
        ticket = Ticket(
            id=self.generate_id(),
            user_id=query.user_id,
            channel=query.channel,
            question=query.content,
            status="open",
            assigned_to=None,
            created_at=datetime.now(),
            updated_at=datetime.now()
        )
        
        self.tickets[ticket.id] = ticket
        
        # 发送创建通知
        await self.notify_ticket_created(ticket)
        
        return ticket
    
    async def assign_ticket(self, ticket_id: str, agent_id: str):
        """分配工单"""
        if ticket_id in self.tickets:
            self.tickets[ticket_id].assigned_to = agent_id
            self.tickets[ticket_id].status = "pending"
            self.tickets[ticket_id].updated_at = datetime.now()
    
    async def resolve_ticket(self, ticket_id: str):
        """解决工单"""
        if ticket_id in self.tickets:
            self.tickets[ticket_id].status = "resolved"
            self.tickets[ticket_id].updated_at = datetime.now()
    
    async def notify_ticket_created(self, ticket: Ticket):
        """通知工单创建"""
        message = f"""📝 新工单创建
工单号: {ticket.id}
用户: {ticket.user_id}
渠道: {ticket.channel}
问题: {ticket.question}"""
        print(message)
```

---

## 常见问题

| 问题 | 解答 |
|------|------|
| FAQ 匹配不准确 | 调整 keywords 和 confidence_threshold，确保关键词覆盖同义词 |
| 转人工没反应 | 检查 internal 渠道配置，确认通知渠道可用 |
| 多渠道消息格式不同 | 使用 ChannelAdapter 适配每个平台的消息格式 |
| AI 回答质量差 | 调整 model 配置或添加更多 examples 到 prompt |
| 置信度阈值如何设置 | 根据实际效果调整，0.3-0.5 是合理范围 |

---

## 本章小结

| 要点 | 内容 |
|------|------|
| 核心功能 | 多渠道接入 + 意图识别 + FAQ 匹配 + 转人工 |
| 关键技术 | FAQKnowledgeBase + Intent Classification + ChannelAdapter |
| 架构设计 | 解耦的适配器模式支持灵活扩展新渠道 |
| 可扩展点 | 满意度评价、多轮对话、工单系统 |

## 延伸阅读

- 第 2.3 章《Memory 记忆系统》- 了解知识库持久化
- 第 2.5 章《Channel 消息渠道》- 多渠道消息管理
- 第 2.6 章《Guard 安全系统》- 敏感信息过滤与安全
- 第 2.7 章《Skill 技能系统》- 自定义技能扩展
