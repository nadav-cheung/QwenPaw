# 多智能体协作

## 本章导读

| 项目 | 内容 |
|------|------|
| **学习目标** | 完成本章后，你能够：1) 描述多智能体协作的架构模型 2) 解释 ACP 协议的通信机制 3) 实现智能体间的任务委托和消息传递 4) 分析 MultiAgentManager 的管理策略 |
| **前置知识** | [07-智能体核心架构](./07-智能体核心架构.md)、[12-记忆系统深入](./12-记忆系统深入.md) |
| **预计时长** | 50 分钟（阅读 30 分钟 + 练习 20 分钟） |
| **难度等级** | ⭐⭐⭐⭐ |
| **核心关键词** | `ACP` `多智能体` `委托` `协作` |

> **一句话概述**：讲解 QwenPaw 的多智能体协作机制，包括 ACP 协议、任务委托和 MultiAgentManager 的管理策略。

## 概述

QwenPaw 支持多智能体协作，通过 ACP（Agent Communication Protocol）协议实现智能体间的通信和任务委托。本教程深入讲解 ACP 协议架构、多智能体协作机制、委托工具和协作模式。

---

## 0. MultiAgentManager — 多智能体生命周期管理

源码路径：`src/qwenpaw/app/multi_agent_manager.py:22`

### 0.1 类定义与核心特性

```python
# src/qwenpaw/app/multi_agent_manager.py:22
class MultiAgentManager:
    """管理多个智能体工作空间。

    特性：
    - 延迟加载：工作空间仅在首次请求时创建
    - 生命周期管理：启动、停止、重新加载工作空间
    - 线程安全：使用异步锁进行并发访问
    - 热重载：重新加载单个工作空间而不影响其他
    - 并行启动：通过细粒度锁实现多个智能体并发启动
      （在慢速工作空间初始化期间释放锁）
    """
```

### 0.2 get_agent 延迟加载与去重

源码路径：`src/qwenpaw/app/multi_agent_manager.py:42`

```python
# src/qwenpaw/app/multi_agent_manager.py:42
async def get_agent(self, agent_id: str) -> Workspace:
    """通过 ID 获取智能体工作空间（带去重的延迟加载）。

    如果工作空间不在内存中，则创建并启动。
    相同 agent_id 的多个并发调用者会被协调：
    第一个调用者创建工作空间，其他等待。

    锁仅在字典检查/变更时短暂持有，不在工作空间
    启动期间持有，允许并行智能体初始化。
    """
    # 快速路径：已加载（无需锁）
    if agent_id in self.agents:
        logger.debug(f"Returning cached agent: {agent_id}")
        return self.agents[agent_id]

    should_start = False
    event = None
    agent_ref = None

    async with self._lock:
        # 在锁下重新检查
        if agent_id in self.agents:
            return self.agents[agent_id]

        if agent_id in self._pending_starts:
            # 另一个任务正在启动此智能体；等待它
            event = self._pending_starts[agent_id]
        else:
            # 我们是第一个调用者 — 验证配置并声明启动
            config = load_config()
            if agent_id not in config.agents.profiles:
                raise ConfigurationException(...)
            agent_ref = config.agents.profiles[agent_id]
            event = asyncio.Event()
            self._pending_starts[agent_id] = event
            should_start = True

    if not should_start:
        # 等待正在进行的启动完成
        await event.wait()
        if agent_id in self.agents:
            return self.agents[agent_id]
        raise ConfigurationException(...)

    # 我们是启动者 — 在锁外创建以实现并行
    instance = Workspace(agent_id=agent_id, workspace_dir=agent_ref.workspace_dir)
    await instance.start()
    instance.set_manager(self)

    async with self._lock:
        self.agents[agent_id] = instance

    return instance
```

### 0.3 零停机重新加载

源码路径：`src/qwenpaw/app/multi_agent_manager.py:138`

```python
# src/qwenpaw/app/multi_agent_manager.py:138
async def _graceful_stop_old_instance(
    self, old_instance: Workspace, agent_id: str
) -> None:
    """检查活跃任务后优雅停止旧实例。

    如果存在活跃任务，在后台安排延迟清理。
    否则立即停止。
    """
    has_active = await old_instance.task_tracker.has_active_tasks()

    if has_active:
        # 活跃任务 — 在后台安排延迟清理
        async def delayed_cleanup():
            try:
                completed = await old_instance.task_tracker.wait_all_done(timeout=60.0)
                if completed:
                    logger.info(f"All tasks completed for old instance {agent_id}.")
                await old_instance.stop(final=False)
            except Exception as e:
                logger.warning(f"Error during delayed cleanup for {agent_id}: {e}.")

        cleanup_task = asyncio.create_task(delayed_cleanup())
        self._cleanup_tasks.add(cleanup_task)
        cleanup_task.add_done_callback(lambda t: self._cleanup_tasks.discard(t))
    else:
        # 无活跃任务 — 立即停止
        await old_instance.stop(final=False)
```

### 0.4 并行启动所有已配置智能体

源码路径：`src/qwenpaw/app/multi_agent_manager.py:454`

```python
# src/qwenpaw/app/multi_agent_manager.py:454
async def start_all_configured_agents(self) -> dict[str, bool]:
    """并发启动配置中定义的所有已启用智能体。

    仅启动 enabled=True 的智能体。
    禁用的智能体被跳过以节省资源。

    智能体真正并行启动：get_agent() 仅在字典检查时
    短暂持有管理器锁，在慢速工作空间初始化期间释放。

    返回：
        dict[str, bool]: agent_id 到成功状态的映射
    """
    config = load_config()
    enabled_agents = {
        agent_id: ref
        for agent_id, ref in config.agents.profiles.items()
        if getattr(ref, "enabled", True)
    }
    agent_ids = list(enabled_agents.keys())

    async def start_single_agent(agent_id: str) -> tuple[str, bool]:
        try:
            await self.get_agent(agent_id)
            return (agent_id, True)
        except Exception as e:
            logger.error(f"Failed to start agent {agent_id}: {e}")
            return (agent_id, False)

    # 真正并行：get_agent 在工作空间启动期间释放锁
    results = await asyncio.gather(
        *[start_single_agent(agent_id) for agent_id in agent_ids],
        return_exceptions=False,
    )
    return dict(results)
```

### 0.5 关键设计模式

| 模式 | 说明 |
|------|------|
| 双重检查锁定 | 先无锁检查，再在锁内重新检查，确保线程安全且高性能 |
| 延迟初始化 | 工作空间仅在首次请求时创建，避免预加载开销 |
| 细粒度锁 | 锁仅在字典操作时持有，不在工作空间创建期间持有 |
| 零停机重载 | 新实例立即服务请求，旧实例在后台等待任务完成后清理 |

---

## 1. ACP 协议概述

ACP（Agent Communication Protocol）是 QwenPaw 用于智能体间通信的标准化协议，基于 stdio 子进程通信，支持会话管理、权限挂起（SuspendedPermission）和错误处理。核心组件包括 `ACPService`（服务单例）、`ACPHostedClient`（托管客户端）和 `_Conversation`（会话状态），源码位于 `src/qwenpaw/agents/acp/`。

> ACP 协议的完整规范、消息格式、权限模型、会话生命周期和传输层细节，详见 [58-ACP智能体通信协议](./58-ACP智能体通信协议.md)。

---

## 2. 内置 ACP 代理

### 2.1 支持的外部代理

| 代理 | 命令 | 说明 |
|------|------|------|
| OpenCode | `opencode acp` | OpenCode IDE |
| Qwen Code | `qwen --acp` | 阿里通义编码 |
| Claude Code | `npx -y @zed-industries/claude-agent-acp` | Claude 编程 |
| Codex | `npx -y @zed-industries/codex-acp` | OpenAI Codex |

### 2.2 ACP 配置

```json
{
  "acp": {
    "agents": {
      "opencode": {
        "enabled": true,
        "command": "opencode",
        "args": ["acp"],
        "env": {},
        "stdio_buffer_limit_bytes": 1048576
      },
      "claude_code": {
        "enabled": true,
        "command": "npx",
        "args": ["-y", "@zed-industries/claude-agent-acp"],
        "env": {
          "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}"
        }
      }
    }
  }
}
```

---

## 3. 委托工具

### 3.1 delegate_external_agent

委托任务给外部 ACP 代理。

**源码路径**: `src/qwenpaw/agents/tools/delegate_external_agent.py`

**工具签名** (第431-589行):

```python
async def delegate_external_agent(
    action: str,
    runner: str,
    message: str = "",
    cwd: str = "",
    max_runtime: Optional[float] = None,
) -> ToolResponse | AsyncGenerator[ToolResponse, None]:
    """
    打开、交流、响应权限或关闭 ACP 智能体会话。
    
    1. 调用 delegate_external_agent(action="start", runner=..., message=...)
       打开新会话
    2. 调用 delegate_external_agent(action="message", runner=..., message=...)
       继续对话
    3. 出现权限请求时，先询问用户选择，然后调用
       delegate_external_agent(action="respond", runner=..., message=...)
       响应待处理的权限请求
    4. 调用 delegate_external_agent(action="close", runner=...)
       结束会话
    """
```

### 3.2 协作工具集

| 工具 | 功能 |
|------|------|
| `delegate_external_agent` | 委托任务给其他智能体 |
| `chat_with_agent` | 与其他智能体对话 |
| `submit_to_agent` | 提交任务给智能体 |
| `check_agent_task` | 检查任务状态 |
| `list_agents` | 列出可用智能体 |

---

## 4. 多智能体协作模式

### 4.1 主从模式

```
┌──────────────┐         委托          ┌──────────────┐
│   Master     │ ─────────────────────►│   Worker     │
│   Agent      │ ◄─────────────────────│   Agent      │
│  (任务分解)   │         结果          │  (执行任务)   │
└──────────────┘                       └──────────────┘
```

```python
# 主从协作示例
async def master_worker_collaboration(task: str):
    # 1. 主智能体分解任务
    subtasks = await master.decompose_task(task)
    
    # 2. 并行委托给工作智能体
    results = await asyncio.gather(*[
        delegate_external_agent("worker_1", subtask)
        for subtask in subtasks
    ])
    
    # 3. 主智能体汇总结果
    final_result = await master.aggregate_results(results)
    
    return final_result
```

### 4.2 流水线模式

```
┌──────────────┐    阶段1结果    ┌──────────────┐
│   Stage 1   │ ───────────────►│   Stage 2   │
│   Agent      │                 │   Agent      │
│  (数据收集)   │                 │  (数据处理)   │
└──────────────┘                 └──────────────┘
                                            │
                                            ▼
                                       ┌──────────────┐
                                       │   Stage 3   │
                                       │   Agent      │
                                       │  (结果输出)  │
                                       └──────────────┘
```

### 4.3 辩论模式

```
┌──────────────┐                    ┌──────────────┐
│   Agent A   │ ◄────── 反驳 ──────│   Agent B   │
│  (正方)      │                    │  (反方)      │
└──────────────┘                    └──────────────┘
        │                                  │
        └──────────────► 仲裁 ◄────────────┘
                         │
                    ┌──────────────┐
                    │   Master     │
                    │  (裁决)       │
                    └──────────────┘
```

---

## 5. 多智能体配置

### 5.1 工作空间隔离

每个智能体运行在独立的工作空间：

```
workspaces/
├── agent_001/           # 智能体 1 的工作空间
│   ├── skills/         # 技能
│   ├── memory/         # 记忆
│   ├── config.json     # 配置
│   └── ...
├── agent_002/           # 智能体 2 的工作空间
│   ├── skills/
│   ├── memory/
│   └── ...
└── ...
```

### 5.2 智能体配置

```json
{
  "agents": {
    "agent_001": {
      "name": "数据分析师",
      "description": "负责数据分析任务",
      "model": {
        "provider_id": "openai",
        "model": "gpt-4o"
      },
      "skills": ["python", "data_analysis"],
      "channels": ["console"]
    },
    "agent_002": {
      "name": "代码助手",
      "description": "负责编程任务",
      "model": {
        "provider_id": "anthropic",
        "model": "claude-3-5-sonnet"
      },
      "skills": ["coding", "code_review"],
      "channels": ["discord", "telegram"]
    }
  }
}
```

---

## 6. 协作命令

### 6.1 委托命令

```bash
# 在对话中委托任务
/delegateto worker_1 分析这份销售数据

# 指定工作目录
/delegateto coder --cwd /path/to/project 实现登录功能

# 带额外指令
/delegateto reviewer --extra "重点检查安全性" 审查这段代码
```

### 6.2 协作命令

```bash
# 查看可用智能体
/listagents

# 与特定智能体对话
/chatwith agent_002 你好，请帮我分析这个问题

# 检查任务状态
/checkagenttask task_123

# 停止任务
/stopagenttask task_123
```

---

## 7. 协作技能

### 7.1 chat_with_agent 技能

```markdown
# 与其他智能体对话

## 功能
与其他 QwenPaw 智能体进行对话交流。

## 使用方法

```
/chat_with_agent <agent_id> <message>
```

## 示例
```
/chat_with_agent data_analyst 请分析 Q1 销售趋势
```
```

### 7.2 multi_agent_collaboration 技能

```markdown
# 多智能体协作

## 功能
协调多个智能体共同完成复杂任务。

## 协作模式

### 主从模式
一个主智能体分解任务，多个工作智能体并行执行。

### 流水线模式
多个智能体按阶段顺序处理，每个阶段输出作为下一阶段输入。

### 辩论模式
多个智能体对同一问题提出不同观点，由裁决智能体决定。

## 使用方法

---

## 练习题

### 练习一：创建 ACP 会话并委托任务

编写代码使用 `ACPService` 创建会话并委托任务给外部智能体。

```python
import asyncio
from qwenpaw.agents.acp.service import ACPService
from qwenpaw.config import load_config

async def delegate_task():
    config = load_config()
    service = ACPService(config=config.acp)

    # 启动外部智能体会话
    result = await service.run_turn(
        chat_id="chat_001",
        agent="opencode",
        prompt_blocks=[{"type": "text", "content": "分析这个Python项目的结构"}],
        cwd="/path/to/project",
    )
    print(result)
    return result

asyncio.run(delegate_task())
```

**要求**：
1. 配置外部智能体连接信息
2. 处理会话创建失败的情况
3. 添加超时控制防止无限等待

---

### 练习二：处理权限挂起请求

实现一个处理器来响应 ACP 智能体的权限请求。

```python
async def handle_permission_request(acp_session_id: str, option_id: str):
    service = ACPService(config=config.acp)

    async def on_message(msg):
        print(f"Received: {msg}")

    # 用户选择后恢复权限
    result = await service.resume_permission(
        acp_session_id=acp_session_id,
        option_id=option_id,
        on_message=on_message,
    )
    return result
```

**要求**：
1. 模拟权限请求的完整流程
2. 实现选项的展示和选择逻辑
3. 处理用户拒绝权限的情况

---

### 练习三：实现主从协作模式

使用 `delegate_external_agent` 实现并行任务执行。

```python
async def master_worker_pattern(tasks: list[str]):
    """主从模式：主智能体分解任务并行委托给工作智能体"""
    results = await asyncio.gather(*[
        delegate_external_agent(
            action="message",
            runner="worker_1",
            message=task
        )
        for task in tasks
    ], return_exceptions=True)

    successful = [r for r in results if not isinstance(r, Exception)]
    failed = [r for r in results if isinstance(r, Exception)]

    return {"success": successful, "failed": failed}
```

**要求**：
1. 实现任务分解逻辑
2. 处理工作智能体返回的错误
3. 实现结果聚合

---

### 练习四：ACP 会话生命周期管理

管理多个 ACP 会话的完整生命周期。

```python
class ACPSessionManager:
    def __init__(self):
        self.sessions: dict[str, _Conversation] = {}
        self.service = ACPService(config=load_config().acp)

    async def create_session(self, chat_id: str, agent: str, cwd: str):
        session = await self.service.run_turn(
            chat_id=chat_id,
            agent=agent,
            prompt_blocks=[],
            cwd=cwd,
        )
        self.sessions[chat_id] = session
        return session

    async def close_session(self, chat_id: str, agent: str):
        await self.service.close_chat_session(chat_id=chat_id, agent=agent)
        self.sessions.pop(chat_id, None)

    async def list_sessions(self):
        return list(self.sessions.keys())
```

**要求**：
1. 实现会话的创建、查询、关闭
2. 添加会话状态检查
3. 实现零停机重载机制

---

## 8. 如果你来自 Java...

### 8.1 多智能体协作对比 Java Agent 框架

| 特性 | QwenPaw MAS | Java Multi-Agent (JADE/Msip) |
|------|-------------|------------------------------|
| 通信协议 | ACP (自定义) | FIPA ACL (标准) |
| 消息格式 | JSON | SL0/ACL XML |
| 代理框架 | 自定义 | JADE (开源) |
| 任务委托 | 委托工具 | Contract Net Protocol |
| 状态管理 | Workspace 隔离 | Agent Container |

### 8.2 ACP vs FIPA ACL

**FIPA ACL（Java JADE）：**
```java
// Java - FIPA ACL 消息
ACLMessage message = new ACLMessage(ACLMessage.INFORM);
message.setOntology("weather");
message.setContent("(temperature 25)");
message.addReceiver(new AID("weather-agent", AID.ISLOCALNAME));
```

**ACP（QwenPaw）：**
```python
# Python - ACP 消息
result = await delegate_external_agent(
    action="start",
    runner="weather_agent",
    message="What's the weather?"
)
```

### 8.3 代理架构对比

| 概念 | Java (JADE) | Python (QwenPaw) |
|------|-------------|------------------|
| Agent | Jade.core.Agent | Workspace class |
| 行为 | Behaviour | Skill/Tool |
| 消息 | ACLMessage | dict (JSON) |
| 容器 | Container | Workspace |
| 黄页 | DF (Directory Facilitator) | Agent Registry |

### 8.4 主从模式的等价实现

**Java（传统微服务模式）：**
```java
// Java - ExecutorService 实现主从模式
ExecutorService executor = Executors.newFixedThreadPool(workers);

List<Future<Result>> futures = tasks.stream()
    .map(task -> executor.submit(() -> processTask(task)))
    .collect(Collectors.toList());

List<Result> results = futures.stream()
    .map(f -> f.get())
    .collect(Collectors.toList());
```

**等价 QwenPaw：**
```python
# Python - asyncio.gather 实现并行委托
results = await asyncio.gather(*[
    delegate_external_agent("worker_1", subtask)
    for subtask in subtasks
])
```

### 8.5 编排器对比

| 框架 | 编排方式 | Java 示例 | Python 示例 |
|------|----------|-----------|-------------|
| QwenPaw | ACP 委托 | - | `delegate_external_agent()` |
| Java Spring | @Async + Future | `@Async` 注解 | `asyncio.gather()` |
| Akka | Actor 消息 | `actor.tell()` | `asyncio.send()` |
| JADE | ACL 消息 | `agent.send(acl)` | ACP 协议 |

### 8.6 QwenPaw 协作模式与 Java 设计模式的对应

| QwenPaw 模式 | Java 设计模式 | 说明 |
|--------------|---------------|------|
| 主从模式 | Master-Slave | 任务分解 + 并行执行 |
| 流水线模式 | Pipeline | 职责链 + 过滤器 |
| 辩论模式 | Strategy + Mediator | 多策略 + 仲裁者 |

### 8.7 关键差异

1. **通信方式**：Java 常用同步 RMI/gRPC，QwenPaw ACP 是异步消息
2. **状态隔离**：Java 代理共享内存，QwenPaw 每个 Workspace 独立文件系统
3. **生命周期**：Java 代理常驻，QwenPaw Workspace 可按需创建销毁
4. **工具调用**：QwenPaw 工具是 AI 感知的，Java RMI 只是远程方法

```bash
/multi_agent_collaboration --pattern <pattern> --task <task>
```

## 示例
```
/multi_agent_collaboration --pattern pipeline --task "处理数据并生成报告"
```
```

---

## 9. 权限与安全

### 9.1 委托权限控制

```python
class ACPPermission:
    """ACP 权限控制"""
    
    # 允许的委托目标
    ALLOWED_AGENTS = ["trusted_agent_1", "trusted_agent_2"]
    
    # 允许的操作
    ALLOWED_OPERATIONS = ["read", "analyze", "process"]
    
    # 敏感操作需要确认
    SENSITIVE_OPERATIONS = ["write", "delete", "execute"]
```

### 9.2 权限请求流程

```
委托请求
    │
    ▼
检查权限配置
    │
    ├─── 允许 ───► 执行委托
    │
    └─── 需要确认 ───► SuspendedPermission
                              │
                              ▼
                        用户审批
                         /    \
                        /      \
                     批准      拒绝
                      │         │
                      ▼         ▼
                   执行       返回错误
```

---

## 10. 故障排查

### 10.1 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 委托超时 | 外部智能体无响应 | 增加 timeout 或检查智能体状态 |
| 权限拒绝 | 权限配置错误 | 检查 ALLOWED_AGENTS 配置 |
| 会话丢失 | 智能体崩溃 | 重新启动智能体 |
| 通信失败 | ACP 协议不兼容 | 检查智能体版本 |

### 10.2 调试技巧

```python
import logging

# 启用 ACP 调试日志
logging.getLogger("qwenpaw.agents.acp").setLevel(logging.DEBUG)
logging.getLogger("qwenpaw.agents.acp.service").setLevel(logging.DEBUG)
```

### 10.3 诊断命令

```bash
# 查看 ACP 会话
qwenpaw agents acp sessions

# 测试连接
qwenpaw agents acp test --agent opencode

# 关闭会话
qwenpaw agents acp close --session session_id
```

---

## 11. 最佳实践

### 11.1 协作设计原则

1. **单一职责**: 每个智能体专注于特定任务
2. **清晰接口**: 定义明确的输入输出格式
3. **错误处理**: 每个智能体都应该有错误恢复机制
4. **资源管理**: 及时关闭不需要的会话

### 11.2 性能优化

| 策略 | 说明 |
|------|------|
| 并行委托 | 独立任务并行执行，提高效率 |
| 会话复用 | 避免重复创建会话，减少开销 |
| 缓存结果 | 常用结果缓存复用，避免重复计算 |
| 超时控制 | 防止单个任务无限阻塞 |

### 11.3 安全建议

1. **最小授权**: 只授予必要的智能体访问权限
2. **输入验证**: 验证所有委托请求的输入
3. **审计日志**: 记录所有协作活动
4. **隔离运行**: 敏感任务在隔离环境运行

---

## 12. 应用场景

### 12.1 主从协作

主智能体分解复杂任务，委托给多个工作智能体并行执行：
- **场景**: 大型数据分析、批量文件处理、多渠道内容生成
- **架构**: Master Agent → 任务分解 → Worker Agents (并行) → 结果汇总

### 12.2 流水线协作

多个智能体按阶段顺序处理，每个阶段输出作为下一阶段输入：
- **场景**: 数据 ETL、文档处理流水线、多语言翻译
- **架构**: Stage 1 → Stage 2 → Stage 3 → 结果输出

### 12.3 专家咨询

不同领域的专家智能体协作，提供全面解决方案：
- **场景**: 法律咨询、技术方案评审、创意头脑风暴
- **架构**: Coordinator Agent → 专家咨询 → 综合建议

### 12.4 外部工具集成

通过 ACP 委托给 Claude Code、OpenCode 等外部编程智能体：
- **场景**: 复杂代码生成、代码审查、调试协助
- **配置**: 在 acp.agents 中配置外部代理连接信息

---

## 13. 常见问题

### 13.1 委托问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 委托超时 | 外部智能体无响应 | 增加 timeout 或检查智能体状态 |
| 权限拒绝 | 权限配置错误 | 检查 ALLOWED_AGENTS 配置 |
| 会话丢失 | 智能体崩溃 | 重新启动智能体 |
| 通信失败 | ACP 协议不兼容 | 检查智能体版本兼容性 |

### 13.2 调试方法

```bash
# 查看 ACP 会话
qwenpaw agents acp sessions

# 测试连接
qwenpaw agents acp test --agent opencode

# 关闭会话
qwenpaw agents acp close --session session_id

# 启用调试日志
export LOG_LEVEL=DEBUG
grep acp ~/.qwenpaw/logs/qwenpaw.log
```

---

## 14. 总结

### 核心要点

1. **ACP 协议**: 标准化智能体间通信的协议，支持会话管理、权限挂起、错误处理
2. **ACPService 单例**: 管理所有 ACP 会话，提供 run_turn、resume_permission 等核心方法
3. **委托工具**: delegate_external_agent 实现任务委托、消息传递、权限响应
4. **协作模式**: 主从模式、流水线模式、辩论模式等多种协作架构
5. **权限管理**: SuspendedPermission 机制确保敏感操作需要用户确认

### 关键配置

| 配置项 | 说明 |
|--------|------|
| `config.json` acp.agents | 外部代理配置列表 |
| `acp.agents.<name>.command` | 代理启动命令 |
| `acp.agents.<name>.env` | 代理环境变量 |
| `stdio_buffer_limit_bytes` | stdio 缓冲区大小限制 |

### 故障排查流程

```
外部智能体连接失败
    │
    ├─► 检查代理命令: qwenpaw agents acp test --agent <name>
    │
    ├─► 验证环境变量: echo $ANTHROPIC_API_KEY (如需要)
    │
    ├─► 检查端口占用: lsof -i :8088
    │
    └─► 查看详细日志: grep acp ~/.qwenpaw/logs/qwenpaw.log

委托任务无响应
    │
    ├─► 检查会话状态: qwenpaw agents acp sessions
    │
    ├─► 增加超时时间: max_runtime 参数
    │
    └─► 关闭后重新打开会话: action=close 然后 action=start
```

---

## 15. 参考资料

- 源码路径：`src/qwenpaw/agents/acp/`
- ACP 协议规范：https://github.com/agentics/ACP
- 配置模型：`src/qwenpaw/config/config.py`

---

## 实战演练

### 基础练习（⭐）
**目标**: 阅读 MultiAgentManager 源码，列出它支持的消息转发模式
**提示**: 查看 `src/qwenpaw/app/multi_agent_manager.py` 中的 `get_agent` 方法和协作工具列表
**参考思路**: MultiAgentManager 本身管理 Workspace 的生命周期和并发访问，消息转发由协作工具集实现——包括 delegate_external_agent（委托）、chat_with_agent（对话）、submit_to_agent（提交）等模式

### 进阶练习（⭐⭐⭐）
**目标**: 追踪一次 ACP 协议的委托调用：从发起方到接收方的完整消息流
**提示**: 从 `delegate_external_agent` 工具的 `action="start"` 调用开始，跟踪 ACPService 的 `run_turn` 方法
**参考思路**: 发起方调用 `delegate_external_agent(action="start")` → `ACPService` 通过 stdio 启动子进程 → 发送 prompt_blocks → 接收方 Agent 处理并返回结果 → 如遇权限请求则挂起为 SuspendedPermission → 用户审批后通过 `resume_permission` 恢复

### 挑战练习（⭐⭐⭐⭐⭐）
**目标**: 设计一个三智能体协作场景（协调者+研究者+写作者），画出消息流图
**提示**: 参考主从模式和流水线模式的架构，结合 ACP 协议的委托机制
**参考思路**: 协调者（Coordinator）接收用户任务 → 分解子任务 → 通过 `delegate_external_agent` 委托给研究者（Researcher）收集资料 → 研究结果返回协调者 → 再委托给写作者（Writer）生成最终报告 → 协调者汇总回复用户；需要设计合理的 agent_id、workspace 隔离和任务超时策略

## 16. 知识检查

### 题目一

MultiAgentManager 的 `get_agent` 方法如何处理对同一 agent_id 的多个并发请求？这种设计模式叫什么？

<details>
<summary>参考答案</summary>

采用双重检查锁定（Double-Checked Locking）模式配合 `_pending_starts` 字典实现去重。第一次检查在锁外进行快速路径判断；如果未命中，在锁内重新检查。对于同一 agent_id 的并发请求，第一个调用者会创建 `asyncio.Event` 并记录到 `_pending_starts`，后续调用者发现已有 pending 状态后会等待该 Event。这样既保证了线程安全，又避免了重复创建工作空间，同时锁只在字典检查/变更时短暂持有，不会阻塞在工作空间启动期间。
</details>

### 题目二

ACP 协议中的 SuspendedPermission 机制解决了什么问题？如果去掉这个机制，智能体的行为会有什么变化？

<details>
<summary>参考答案</summary>

SuspendedPermission 解决的是外部智能体执行敏感操作时的用户确认问题。当外部 ACP 智能体（如 Claude Code）需要执行文件写入、命令执行等敏感操作时，会发出权限请求。ACPHostedClient 将其封装为 SuspendedPermission 并挂起当前 turn，等待用户通过 /approve 或 /deny 响应。

如果去掉这个机制，外部智能体的所有操作将自动执行，没有任何人工审批环节。这会导致安全风险：恶意或错误的工具调用可能直接修改文件系统、执行危险命令等，而用户毫不知情。
</details>

### 题目三

`_graceful_stop_old_instance` 方法中的零停机重载是如何实现的？为什么旧实例的清理不在锁内进行？

<details>
<summary>参考答案</summary>

零停机重载的实现方式是：先创建并启动新实例，然后立即将其放入 `self.agents` 字典，使新请求路由到新实例。旧实例的停止在锁外异步执行：

- 如果旧实例没有活跃任务，立即调用 `stop()`。
- 如果有活跃任务，启动一个后台 `delayed_cleanup` 协程，等待任务完成（最多 60 秒）后再停止旧实例。

旧实例的清理不在锁内进行，因为 `stop()` 和等待任务完成都可能耗时较长。如果在锁内等待，所有其他智能体的启动和访问请求都会被阻塞，造成系统级延迟。将清理移到锁外，确保管理器锁只用于快速的字典操作。
</details>

---

## 17. 延伸阅读

- [58-ACP智能体通信协议](./58-ACP智能体通信协议.md) -- ACP 协议的完整规范和消息格式
- [ACP 智能体通信协议](./58-ACP智能体通信协议.md) -- ACP 协议传输层、权限模型的深入分析
- [29-Mission模式详解](./29-Mission模式详解.md) -- Mission 模式下多智能体协作的具体应用场景
