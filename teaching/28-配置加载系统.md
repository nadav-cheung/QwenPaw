# 配置加载系统 (Config)

配置系统管理 QwenPaw 的所有配置项，采用分层配置架构，支持环境变量、配置文件、Agent 配置等多来源配置。

## 核心文件

| 文件路径 | 描述 |
|----------|------|
| `src/qwenpaw/config/config.py` | 主配置模块，包含所有 Pydantic 模型 |
| `src/qwenpaw/config/utils.py` | 配置加载/保存工具函数 |
| `src/qwenpaw/config/context.py` | Context 变量（workspace_dir, recent_max_bytes） |
| `src/qwenpaw/config/timezone.py` | 系统时区检测 |
| `src/qwenpaw/constant.py` | 环境变量加载器和常量定义 |

## 主要配置模型

| 类名 | 行号 | 用途 |
|------|------|------|
| `Config` | 1409 | 根配置模型 |
| `AgentsConfig` | 928 | Agent 配置（根配置） |
| `AgentProfileConfig` | 859 | 单个 Agent 完整配置（workspace/agent.json） |
| `AgentProfileRef` | 839 | Agent 引用（根配置中只有 ID 和路径） |
| `AgentsRunningConfig` | 647 | Agent 运行时配置 |
| `ChannelConfig` | 387 | 渠道配置容器 |
| `BaseChannelConfig` | 192 | 渠道配置基类 |
| `MCPConfig` / `MCPClientConfig` | 1088/1012 | MCP 客户端配置 |
| `ToolsConfig` / `BuiltinToolConfig` | 1257/1109 | 工具配置 |
| `SecurityConfig` / `ToolGuardConfig` | 1399/1338 | 安全配置 |
| `HeartbeatConfig` | 421 | 心跳配置 |
| `MemorySummaryConfig` | 570 | 记忆摘要配置 |
| `ACPConfig` / `ACPAgentConfig` | 105/56 | ACP 协议配置 |
| `ModelSlotConfig` | 43 | 模型槽位配置 |

## 配置加载优先级

**从高到低**：
1. **环境变量** (通过 `EnvVarLoader` 类)
2. **配置文件** (`config.json`)
3. **Agent 配置文件** (`workspace/agent.json`)
4. **代码默认值** (Pydantic Field defaults)

## 环境变量加载

**文件**: `src/qwenpaw/constant.py:12-52`

```python
class EnvVarLoader:
    @staticmethod
    def get_bool(env_var: str, default: bool = False) -> bool: ...
    @staticmethod
    def get_float(env_var: str, default: float = 0.0, ...) -> float: ...
    @staticmethod
    def get_int(env_var: str, default: int = 0, ...) -> int: ...
    @staticmethod
    def get_str(env_var: str, default: str = "") -> str: ...
```

### 关键环境变量

| 环境变量 | 用途 |
|----------|------|
| `QWENPAW_WORKING_DIR` | 工作目录（默认 `~/.qwenpaw` 或 `~/.copaw`） |
| `QWENPAW_SECRET_DIR` | 密钥目录 |
| `QWENPAW_ENABLED_CHANNELS` / `QWENPAW_DISABLED_CHANNELS` | 启用的渠道 |
| `QWENPAW_RUNNING_IN_CONTAINER` | 容器环境标识 |
| `DASHSCOPE_BASE_URL` | API 基础 URL |
| `QWENPAW_LLM_*` | LLM 相关配置（重试、并发、QPM 等） |

**Legacy 兼容**：`COPAW_*` 环境变量自动作为 `QWENPAW_*` 的回退

## 配置文件加载

**文件**: `src/qwenpaw/config/utils.py:290-360`

```python
def load_config(config_path: Optional[Path] = None) -> Config:
    """Load config from file. Returns default Config if file is missing."""
    if config_path is None:
        config_path = get_config_path()
    if not config_path.is_file():
        return Config()  # 返回默认配置

    data = _read_config_data(config_path)  # 使用 json_repair 处理损坏的 JSON
    if data is None:
        return Config()

    data = _normalize_working_dir_bound_paths(data)  # 路径规范化
    # 向后兼容：last_api_host/last_api_port -> last_api
    return Config.model_validate(data)
```

配置文件路径：`WORKING_DIR/config.json`

## Agent 配置加载

**文件**: `src/qwenpaw/config/config.py:1490-1530`

```python
def load_agent_config(agent_id: str) -> AgentProfileConfig:
    config = load_config()
    agent_ref = config.agents.profiles[agent_id]
    workspace_dir = Path(agent_ref.workspace_dir).expanduser()
    agent_config_path = workspace_dir / "agent.json"

    if not agent_config_path.exists():
        fallback_config = build_fallback_agent_profile_config(agent_id, config)
        save_agent_config(agent_id, fallback_config)  # 保存默认值
        return fallback_config

    return AgentProfileConfig(**data)
```

Agent 配置路径：`{workspace_dir}/agent.json`

## 根配置模型

**文件**: `src/qwenpaw/config/config.py:1409-1430`

```python
class Config(BaseModel):
    """Root config (config.json)."""
    channels: ChannelConfig = ChannelConfig()
    mcp: MCPConfig = MCPConfig()
    tools: ToolsConfig = Field(default_factory=ToolsConfig)
    last_api: LastApiConfig = LastApiConfig()
    agents: AgentsConfig = Field(default_factory=AgentsConfig)
    last_dispatch: Optional[LastDispatchConfig] = None
    security: SecurityConfig = Field(default_factory=SecurityConfig)
    acp: ACPConfig = Field(default_factory=ACPConfig)
    show_tool_details: bool = True
    user_timezone: str = Field(
        default_factory=detect_system_timezone,
        description="User IANA timezone (e.g. Asia/Shanghai). "
        "Defaults to the system timezone.",
    )
    plugins: Dict[str, Dict[str, Any]] = Field(
        default_factory=dict,
        description="Plugin configurations.",
    )
```

## 配置模型关系图

```
Config (根配置 config.json)
├── channels: ChannelConfig
│   ├── imessage, discord, dingtalk, feishu, qq, telegram...
│   └── (每个渠道继承 BaseChannelConfig)
├── mcp: MCPConfig
│   └── clients: Dict[str, MCPClientConfig]
├── tools: ToolsConfig
│   └── builtin_tools: Dict[str, BuiltinToolConfig]
├── agents: AgentsConfig
│   ├── active_agent: str
│   ├── profiles: Dict[str, AgentProfileRef]  # 引用
│   └── (legacy) running, llm_routing, language...
├── security: SecurityConfig
│   ├── tool_guard: ToolGuardConfig
│   ├── file_guard: FileGuardConfig
│   └── skill_scanner: SkillScannerConfig
├── acp: ACPConfig
│   └── agents: Dict[str, ACPAgentConfig]
├── last_api: LastApiConfig
├── last_dispatch: LastDispatchConfig
└── plugins: Dict[str, Dict[str, Any]]

AgentProfileConfig (workspace/agent.json)
├── channels: Optional[ChannelConfig]
├── mcp: Optional[MCPConfig]
├── tools: Optional[ToolsConfig]
├── security: Optional[SecurityConfig]
├── running: AgentsRunningConfig
├── llm_routing: AgentsLLMRoutingConfig
├── heartbeat: Optional[HeartbeatConfig]
├── language: str
└── system_prompt_files: List[str]
```

## 运行时配置

**文件**: `src/qwenpaw/config/config.py:647-720`

```python
class AgentsRunningConfig(BaseModel):
    max_iters: int = Field(default=100, ge=1)
    auto_continue_on_text_only: bool = Field(default=False)
    llm_retry_enabled: bool = Field(default=LLM_MAX_RETRIES > 0)
    llm_max_retries: int = Field(default=max(LLM_MAX_RETRIES, 1), ge=1)
    llm_backoff_base: float = Field(default=LLM_BACKOFF_BASE, ge=0.1)
    llm_backoff_cap: float = Field(default=LLM_BACKOFF_CAP, ge=0.5)
    llm_max_concurrent: int = Field(default=LLM_MAX_CONCURRENT, ge=1)
```

## 记忆摘要配置

**文件**: `src/qwenpaw/config/config.py:570-645`

```python
class MemorySummaryConfig(BaseModel):
    memory_summary_enabled: bool = Field(default=True)
    memory_prompt_enabled: bool = Field(default=True)
    dream_cron: str = Field(default="0 23 * * *")  # 记忆优化 cron
    force_memory_search: bool = Field(default=False)
    force_max_results: int = Field(default=1, ge=1)
    rebuild_memory_index_on_start: bool = Field(default=False)
    recursive_file_watcher: bool = Field(default=False)
```

## 心跳配置

**文件**: `src/qwenpaw/config/config.py:421-433`

```python
class HeartbeatConfig(BaseModel):
    enabled: bool = Field(default=False)
    every: str = Field(default=HEARTBEAT_DEFAULT_EVERY)  # "6h"
    target: str = Field(default=HEARTBEAT_DEFAULT_TARGET)  # "main"
    active_hours: Optional[ActiveHoursConfig] = None
```

## 路径规范化

**文件**: `src/qwenpaw/config/utils.py`

```python
def _normalize_working_dir_bound_paths(data: object) -> object:
    """将旧 ~/.copaw 路径转换为当前 WORKING_DIR"""
    legacy_root_tilde = "~/.copaw"
    legacy_root_abs = str(Path(legacy_root_tilde).expanduser().resolve())
    new_root_abs = str(WORKING_DIR)
    # 重写 workspace_dir, media_dir 字段
```

## 系统时区检测

**文件**: `src/qwenpaw/config/timezone.py`

```python
def detect_system_timezone() -> str:
    """返回 IANA 时区名，失败时回退到 UTC"""
    try:
        return _detect_system_timezone_inner()
    except Exception:
        return "UTC"

def _detect_system_timezone_inner() -> str:
    # 按优先级探测：Python > TZ环境变量 > Windows注册表 > /etc/timezone > /etc/localtime > timedatectl
```

## 内置工具默认配置

**文件**: `src/qwenpaw/config/config.py:1130-1240`

```python
def _default_builtin_tools() -> Dict[str, BuiltinToolConfig]:
    return {
        "execute_shell_command": BuiltinToolConfig(name="execute_shell_command", enabled=True, ...),
        "read_file": BuiltinToolConfig(name="read_file", enabled=True, ...),
        "write_file": BuiltinToolConfig(name="write_file", enabled=True, ...),
        "edit_file": BuiltinToolConfig(name="edit_file", enabled=True, ...),
        "grep_search": BuiltinToolConfig(name="grep_search", enabled=True, ...),
        "glob_search": BuiltinToolConfig(name="glob_search", enabled=True, ...),
        "browser_use": BuiltinToolConfig(name="browser_use", enabled=True, ...),
        # ... 更多工具
    }
```

## 与其他模块的交互

| 模块 | 导入的函数/类 | 用途 |
|------|---------------|------|
| `app/_app.py` | `load_config`, `get_config_path` | 应用初始化 |
| `app/routers/config.py` | `load_config`, `save_config` | 配置 API 路由 |
| `app/routers/agents.py` | `ChannelConfig` | Agent 路由 |
| `app/routers/tools.py` | `load_config`, `load_agent_config` | 工具相关路由 |
| `app/routers/mcp.py` | `MCPClientConfig`, `save_agent_config` | MCP 管理路由 |
| `app/agent_context.py` | `load_config` | Agent 上下文 |
| `app/multi_agent_manager.py` | `load_config` | 多 Agent 管理 |
| `app/channels/manager.py` | `get_channel_registry` | 渠道管理 |

## 核心设计模式

1. **分层配置**：根配置引用 Agent，Agent 存储完整配置
2. **向后兼容**：Legacy 字段自动迁移和合并
3. **容错设计**：`json_repair` 自动修复损坏的 JSON
4. **环境变量优先**：所有配置都可通过环境变量覆盖
5. **默认值链**：环境变量 → 配置文件 → 代码默认值
6. **路径规范化**：自动处理旧版本路径迁移
