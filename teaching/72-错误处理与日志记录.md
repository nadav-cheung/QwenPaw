# 错误处理与日志记录

## 本章导读

| 项目 | 内容 |
|------|------|
| **学习目标** | 完成本章后，你能够：1) 分析异常处理层次结构 2) 理解错误传播和恢复机制 3) 配置错误通知和日志 |
| **前置知识** | [07-智能体核心架构](./07-智能体核心架构.md) |
| **预计时长** | 30 分钟（阅读 20 分钟 + 练习 10 分钟） |
| **难度等级** | ⭐⭐⭐ |
| **核心关键词** | `异常处理` `错误恢复` `日志` |

> **一句话概述**：本章详解 QwenPaw 的多层错误处理策略，包括 QueryErrorDump 上下文转储、异常转换器的语义映射、SSE 流式错误传递以及日志系统的配置与轮转机制。

## 概述

QwenPaw 的错误处理采用多层策略：QueryErrorDump 转储错误上下文、标准化错误响应、SSE 流式错误传递。日志系统支持彩色终端输出和日志轮转。

`★ Insight ─────────────────────────────────────`
- QueryErrorDump 是**事后调试**的关键——异常发生时的完整上下文（堆栈、请求、Agent 状态）全部持久化到 JSON
- 异常转换器 `convert_model_exception` 是**统一对外错误契约**的核心：根据 status_code 和关键词将底层 Provider 异常映射为语义明确的业务异常
- 日志的 `propagate=False` 设计是**隔离依赖日志**的妙招：避免 uvicorn 等第三方库的日志污染
`─────────────────────────────────────────────────`

---

## 1. QueryErrorDump

**源码路径**: `src/qwenpaw/app/runner/query_error_dump.py:48`

### 1.1 功能说明

当 Agent 处理请求发生异常时，QueryErrorDump 将完整的错误上下文转储到临时 JSON 文件，供事后分析和调试。

### 1.2 转储内容

**源码路径**: `src/qwenpaw/app/runner/query_error_dump.py:48`

```python
def write_query_error_dump(
    request: Any,
    exc: BaseException,
    locals_: dict,
) -> str | None:
    """Write error log, traceback and agent/memory state to a temp JSON file.

    Returns the temp file path, or None if write failed.
    """
    payload = {
        "trace": trace_str,                    # 完整堆栈跟踪
        "exception_type": type(exc).__name__,   # 异常类型
        "exception_message": str(exc),           # 异常消息
        "request_info": request_info,           # 请求基本信息
        "request": request_full,                 # 完整请求（脱敏后）
        "agent_state": agent_state,             # Agent 状态
        "ts_utc": datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ"),
    }
```

### 1.3 转储字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `trace` | `str` | `traceback.format_exc()` 完整堆栈 |
| `exception_type` | `str` | 异常类名（如 `ValueError`） |
| `exception_message` | `str` | 异常消息 |
| `request_info` | `dict` | session_id, user_id, channel |
| `request` | `dict` | AgentRequest 序列化（脱敏后） |
| `agent_state` | `dict` | Agent state_dict（如果可用） |
| `ts_utc` | `str` | UTC 时间戳 |

### 1.4 安全序列化

**源码路径**: `src/qwenpaw/app/runner/query_error_dump.py:17`

```python
def _safe_json_serialize(obj: object) -> object:
    """Convert object to JSON-serializable form; use str() for unknowns."""
    if obj is None or isinstance(obj, (bool, int, float, str)):
        return obj
    if isinstance(obj, (list, tuple)):
        return [_safe_json_serialize(x) for x in obj]
    if isinstance(obj, dict):
        return {str(k): _safe_json_serialize(v) for k, v in obj.items()}
    return str(obj)
```

---

## 2. 异常类体系

**源码路径**: `src/qwenpaw/exceptions.py`

### 2.1 QwenPaw 业务异常

```python
class ProviderError(AgentRuntimeErrorException):
    """模型 Provider 错误"""

class ModelFormatterError(AgentRuntimeErrorException):
    """模型消息格式化错误"""

class SystemCommandException(AgentRuntimeErrorException):
    """系统命令执行错误"""

class ChannelError(ExternalServiceException):
    """渠道通信错误"""

class AgentStateError(AgentRuntimeErrorException):
    """Agent 状态和会话错误"""

class SkillsError(AgentRuntimeErrorException):
    """技能管理错误"""
```

### 2.2 异常转换器

**源码路径**: `src/qwenpaw/exceptions.py:96`

```python
def convert_model_exception(
    exc: Exception,
    model_name: Optional[str] = None,
) -> AgentRuntimeErrorException:
    """将异常转换为 agentscope_runtime 异常。"""
```

**转换逻辑**:

| 条件 | 返回类型 |
|------|----------|
| 非模型相关错误 | `UnknownAgentException` |
| status_code = 401/403 | `UnauthorizedModelAccessException` |
| status_code = 429 | `ModelQuotaExceededException` |
| 包含 "timeout" 关键词 | `ModelTimeoutException` |
| 包含 "context" 关键词 | `ModelContextLengthExceededException` |
| 其他模型相关错误 | `ModelExecutionException` |

---

## 3. Runner 错误处理

**源码路径**: `src/qwenpaw/app/runner/runner.py:720`

### 3.1 错误处理流程

```python
except Exception as e:
    # 1. 获取模型名称
    model_name = None
    if agent and hasattr(agent, "model"):
        model_name = getattr(agent.model, "model_name", None)

    # 2. 转换异常
    converted = convert_model_exception(e, model_name)

    # 3. 写入错误转储
    debug_dump_path = write_query_error_dump(
        request=request,
        exc=converted,
        locals_=locals(),
    )

    # 4. 添加调试路径到异常
    if debug_dump_path:
        setattr(converted, "debug_dump_path", debug_dump_path)
        if hasattr(converted, "add_note"):
            converted.add_note(f"(Details:  {debug_dump_path})")

    # 5. 记录日志
    logger.exception(f"Error in query handler: {converted}{path_hint}")

    raise converted from e
```

### 3.2 SSE 错误传递

```python
# 错误通过 SSE 流式传递
yield {
    "type": "error",
    "error": str(exc),
    "dump_id": debug_dump_path,  # 错误转储文件路径
}, False
```

---

## 4. 日志系统

**源码路径**: `src/qwenpaw/utils/logging.py`

### 4.1 日志配置

```python
# 日志命名空间
LOG_NAMESPACE = PROJECT_NAME.lower()  # "qwenpaw"

# 日志文件路径
LOG_FILE_BASENAME = f"{LOG_NAMESPACE}.log"
LOG_FILE_PATH = WORKING_DIR / LOG_FILE_BASENAME

# 日志轮转配置
_LOG_MAX_BYTES = 5 * 1024 * 1024  # 5 MiB
_LOG_BACKUP_COUNT = 3
```

### 4.2 ColorFormatter

**源码路径**: `src/qwenpaw/utils/logging.py:52`

```python
class ColorFormatter(logging.Formatter):
    COLORS = {
        logging.DEBUG: "\033[34m",      # 蓝色
        logging.INFO: "\033[32m",        # 绿色
        logging.WARNING: "\033[33m",     # 黄色
        logging.ERROR: "\033[31m",      # 红色
        logging.CRITICAL: "\033[41m\033[97m",  # 红底白字
    }
    RESET = "\033[0m"
```

**输出格式**:

```
ERROR  src/qwenpaw/app/runner.py:720 | Error message here
```

### 4.3 PlainFormatter

**源码路径**: `src/qwenpaw/utils/logging.py:75`

```python
class PlainFormatter(logging.Formatter):
    def format(self, record: logging.LogRecord) -> str:
        # 输出格式：
        # 2024-01-15 10:30:45 | ERROR | src/qwenpaw/app/runner.py:720 | Error message
```

### 4.4 日志处理器

| 平台 | 处理器 | 说明 |
|------|--------|------|
| Windows | `FileHandler` | 简单文件日志 |
| Linux | `FileHandler` | 简单文件日志 |
| macOS | `RotatingFileHandler` | 自动轮转（5MB × 3 文件） |

### 4.5 SuppressPathAccessLogFilter

**源码路径**: `src/qwenpaw/utils/logging.py:90`

```python
class SuppressPathAccessLogFilter(logging.Filter):
    """过滤 uvicorn 访问日志中包含特定路径的消息。"""
```

---

## 5. 日志级别映射

**源码路径**: `src/qwenpaw/utils/logging.py:13`

```python
_LEVEL_MAP = {
    "critical": logging.CRITICAL,
    "error": logging.ERROR,
    "warning": logging.WARNING,
    "info": logging.INFO,
    "debug": logging.DEBUG,
}
```

---

## 6. Windows ANSI 支持

**源码路径**: `src/qwenpaw/utils/logging.py:28`

```python
def _enable_windows_ansi() -> None:
    """在 Windows 10+ 上启用 ANSI 转义码支持。"""
    if platform.system() != "Windows":
        return
    try:
        import ctypes
        kernel32 = ctypes.windll.kernel32
        # STD_OUTPUT_HANDLE = -11
        # ENABLE_VIRTUAL_TERMINAL_PROCESSING = 0x0004
        handle = kernel32.GetStdHandle(-11)
        mode = ctypes.c_ulong()
        kernel32.GetConsoleMode(handle, ctypes.byref(mode))
        kernel32.SetConsoleMode(handle, mode.value | 0x0004)
    except Exception:
        pass
```

---

## 7. 日志设置函数

**源码路径**: `src/qwenpaw/utils/logging.py:124`

```python
def setup_logger(level: int | str = logging.INFO):
    """配置日志输出，仅输出本项目日志，不输出依赖日志。"""
    logger = logging.getLogger(LOG_NAMESPACE)
    logger.setLevel(level)
    logger.propagate = False  # 关键：不向上传播，避免第三方库日志污染
    if not logger.handlers:
        handler = logging.StreamHandler(sys.stderr)
        handler.setFormatter(ColorFormatter(...))
        logger.addHandler(handler)
    return logger
```

### 7.1 添加文件处理器

**源码路径**: `src/qwenpaw/utils/logging.py:155`

```python
def add_project_file_handler(log_path: Path) -> None:
    """为守护进程日志添加文件处理器。

    幂等：如果已存在相同路径的处理器，不会重复添加。
    """
```

---

## 8. 应用场景

### 8.1 调试生产环境错误

```python
# 1. 从错误响应中获取 dump_id
# SSE 错误事件: {"type": "error", "dump_id": "/tmp/qwenpaw_error_xxx.json"}

# 2. 读取错误转储文件
import json
with open("/tmp/qwenpaw_error_xxx.json") as f:
    error_dump = json.load(f)

# 3. 分析完整上下文
print(error_dump["trace"])          # 堆栈跟踪
print(error_dump["agent_state"])    # Agent 状态
print(error_dump["request"])        # 原始请求
```

### 8.2 自定义日志输出

```python
from qwenpaw.utils.logging import setup_logger, ColorFormatter

# 初始化自定义级别日志
logger = setup_logger(level="DEBUG")

# 输出带颜色的日志
logger.debug("Debug 信息（蓝色）")
logger.info("Info 信息（绿色）")
logger.warning("警告信息（黄色）")
logger.error("错误信息（红色）")
```

---

## 9. 最佳实践

1. **始终使用异常转换器**：不要直接抛出 Provider 异常，通过 `convert_model_exception()` 转换为统一业务异常
2. **利用 dump_id 调试**：生产环境遇到错误时，优先通过 `dump_id` 获取完整错误上下文
3. **`propagate=False` 隔离**：QwenPaw 的日志设计不污染第三方库日志，调试时注意这一设计意图
4. **脱敏处理**：`write_query_error_dump` 会对敏感字段脱敏，不要在生产环境直接暴露 dump 文件路径
5. **macOS 日志轮转**：守护进程在 macOS 上使用 `RotatingFileHandler`，确保磁盘空间管理

---

## 10. 常见问题

### Q1: 错误转储文件会无限增长吗？

不会。QueryErrorDump 写入系统临时目录（`tempfile.gettempdir()`），由操作系统自动清理。文件名为 `qwenpaw_error_{uuid}.json`。

### Q2: 如何在调试时输出更详细的日志？

```bash
# 启动时指定日志级别
qwenpaw app --log-level debug

# 或设置环境变量
export QWENPAW_LOG_LEVEL=debug
```

### Q3: Windows 上日志颜色不显示？

Windows 10+ 自动启用 ANSI 支持。若仍不显示，检查是否使用了旧版 Windows 或禁用了终端颜色支持。

### Q4: 如何将日志同时输出到文件和控制台？

```python
from qwenpaw.utils.logging import setup_logger, add_project_file_handler

logger = setup_logger(level="DEBUG")
add_project_file_handler("/path/to/app.log")  # 同时写入文件
```

---

## 知识检查

1. `convert_model_exception` 根据 status_code 和关键词将底层异常映射为不同类型的业务异常。为什么 429 状态码映射为 `ModelQuotaExceededException` 而不是通用的 `ModelExecutionException`？
2. `QueryErrorDump` 的 `write_query_error_dump` 函数返回的文件路径在后续处理中如何被使用？SSE 错误事件中的 `dump_id` 字段的作用是什么？
3. `setup_logger` 中 `logger.propagate = False` 的设计意图是什么？如果去掉这个设置，日志输出会发生什么变化？

---

## 11. 总结

| 要点 | 说明 |
|------|------|
| QueryErrorDump | 异常时完整上下文（堆栈、请求、Agent 状态）写入 JSON |
| 异常转换器 | `convert_model_exception()` 将 Provider 异常映射为语义明确的业务异常 |
| SSE 错误传递 | 错误通过 SSE 流式事件传递，`dump_id` 用于事后调试 |
| 日志隔离 | `propagate=False` 确保仅输出 QwenPaw 日志，不污染第三方 |
| 平台差异 | macOS 使用日志轮转，Windows 启用 ANSI 颜色 |
| 相关章节 | [Runner 执行机制](./21-请求处理与Runner.md)、[Provider 系统](./82-Provider系统深度解析.md) |

---

## 🐍 来自 Java 的你

如果你熟悉 Java 的异常处理和日志框架：

| Java 机制 | QwenPaw/Python 实现 | 说明 |
|-----------|---------------------|------|
| `throw` / `throws` | `raise` | 异常抛出 |
| `try-catch-finally` | `try-except-finally` | 异常捕获 |
| `Exception` / `RuntimeException` | `Exception` / `BaseException` | 异常层次 |
| `java.util.logging` | `logging` 模块 | 日志框架 |
| `LogManager` | `logging.config.dictConfig` | 日志配置管理 |
| `Thread.dumpStack()` | `traceback.print_exc()` | 堆栈打印 |
| `PrintWriter` / `StringWriter` | `StringIO` | 字符串缓冲区 |

**关键差异概览：**

| 对比项 | Java | Python |
|--------|------|--------|
| 异常声明 | 方法签名中 `throws` 声明 | 无需声明，动态抛出 |
| 异常捕获 | 分类型 catch | 分类型 except |
| 日志级别 | FINEST → WARNING (7级) | DEBUG → CRITICAL (5级) |
| 日志配置 | XML/JSON 配置文件 | Python dict 或 fileConfig |
| 异常链 | `cause.initCause()` / 构造器参数 | `raise E() from e` |

**异常处理对比：**

```java
// Java: 检查型异常
public void readFile() throws IOException {
    try {
        BufferedReader reader = new BufferedReader(new FileReader("file.txt"));
        String line = reader.readLine();
        reader.close();
    } catch (IOException e) {
        logger.error("读取失败", e);
        throw new RuntimeException("文件读取错误", e);  // 异常链
    } finally {
        // 清理资源
    }
}
```

```python
# Python: 非检查型异常
def read_file():
    try:
        with open("file.txt") as f:  # with 自动关闭
            return f.readline()
    except FileNotFoundError as e:
        logger.error(f"读取失败: {e}")
        raise RuntimeError("文件读取错误") from e  # 异常链
    # 无需 finally，with 自动处理
```

**日志框架对比：**

```java
// Java: java.util.logging
import java.util.logging.Logger;
import java.util.logging.Level;

Logger logger = Logger.getLogger(MyClass.class.getName());
logger.log(Level.INFO, "User {0} logged in", userId);
logger.log(Level.WARNING, "Retry attempt {0}", attempt);

// 使用 LogManager 配置
LogManager.getLogManager().readConfiguration(new FileInputStream("logging.properties"));
```

```python
# Python: QwenPaw logging
from qwenpaw.utils.logging import setup_logger

logger = setup_logger("info")
logger.info(f"User {user_id} logged in")
logger.warning(f"Retry attempt {attempt}")

# 配置日志轮转 (macOS)
# _LOG_MAX_BYTES = 5 * 1024 * 1024  # 5 MB
# _LOG_BACKUP_COUNT = 3
```

**异常类层次对比：**

```
Java:
Throwable
├── Error (不需要捕获)
│   ├── AssertionError
│   └── OutOfMemoryError
└── Exception
    ├── IOException (检查型)
    └── RuntimeException (非检查型)
        ├── NullPointerException
        └── IllegalArgumentException

Python:
BaseException
├── SystemExit
├── KeyboardInterrupt
└── Exception
    ├── ValueError
    ├── TypeError
    ├── RuntimeError
    └── ... (所有常规异常)
```

**QwenPaw 异常体系：**

```python
# src/qwenpaw/exceptions.py
AgentRuntimeErrorException  # 基类
├── ProviderError           # 模型 Provider 错误
├── ModelFormatterError    # 消息格式化错误
├── SystemCommandException # 系统命令错误
├── ChannelError           # 渠道通信错误
└── SkillsError            # 技能管理错误
```

**错误转储对比：**

```java
// Java: 手动获取堆栈
try {
    // 操作
} catch (Exception e) {
    // 写入日志
    logger.error("Error: ", e);

    // 手动写入错误文件
    StringWriter sw = new StringWriter();
    e.printStackTrace(new PrintWriter(sw));
    Path dumpFile = Paths.get("/tmp/error_" + System.currentTimeMillis() + ".txt");
    Files.write(dumpFile, sw.toString().getBytes());
}
```

```python
# QwenPaw: 自动错误转储
# QueryErrorDump 自动将完整上下文写入 JSON
# src/qwenpaw/app/runner/query_error_dump.py:48
payload = {
    "trace": trace_str,              # 完整堆栈
    "exception_type": type(exc).__name__,
    "exception_message": str(exc),
    "request": request_full,           # 脱敏后的请求
    "agent_state": agent_state,       # Agent 状态
    "ts_utc": datetime.now().isoformat(),
}
# 返回 dump_id 用于后续调试
```

**Java 开发者注意事项：**

1. **Python 异常是运行时**：不需要在方法签名中声明可能抛出的异常类型
2. **with 语句替代 try-finally**：`with open() as f:` 自动处理资源清理，无需显式 finally
3. **异常链语法**：`raise E() from e` 在 Python 3 中提供原生的异常链支持
4. **日志 propagate=False**：Python 日志默认向上传播（到 root logger），QwenPaw 设置 `propagate=False` 避免第三方库日志污染
5. **错误转储是自动的**：QwenPaw 的 QueryErrorDump 在 Runner 异常处理中自动调用，不需要手动编写

---

## 延伸阅读

- [91-异常处理与恢复](./91-异常处理与恢复.md) -- 深入了解各类异常的恢复策略和降级机制
- [57-日志系统详解](./57-日志系统详解.md) -- 全面掌握日志配置、格式化和轮转机制
- [98-故障排查与诊断](./98-故障排查与诊断.md) -- 学习利用错误转储和日志进行生产环境故障定位
